# Oli Robot Manager 单人敏捷维护手册

## 1. 这套流程解决什么问题

这份手册面向独立维护者。你同时承担产品负责人、开发者、测试人员和发布负责人，但不要在同一时刻扮演所有角色。

推荐节奏：**一周一个 Sprint，同时只开发一个 Issue（WIP = 1）**。

核心规则只有五条：

1. 想法先写 Issue，不直接改 `main`
2. Issue 先写验收标准，再开始编码
3. 一次只做一个 Issue
4. 通过测试和手工验收后才合并
5. 只有满足发布清单才创建版本 Tag

---

## 2. 你的四个角色

| 时间点 | 角色 | 只回答的问题 |
|--------|------|--------------|
| 选需求时 | Product Owner | 哪个问题最影响验收效率或软件可用性？ |
| 写代码时 | Developer | 最小改动是什么？如何自动验证？ |
| 合并前 | Reviewer / Tester | 验收标准真的满足了吗？会伤到机器人吗？ |
| 发布时 | Release Owner | 产物能安装、启动、回滚吗？ |

不要一边写代码一边扩大需求。新想法放回 Backlog，当前 Issue 不加范围。

---

## 3. 一周 Sprint 节奏

### 周一：Planning（30 分钟）

1. 查看开放 Issue
2. 只选一个 `status:ready` 的最高优先级 Issue
3. 确认验收标准、平台、安全影响和“不做什么”
4. 把它加入当前 Milestone
5. 创建分支

```powershell
git switch main
git pull
git switch -c feat/<issue编号>-<简短名称>
```

Bug 使用 `fix/`，文档或流程使用 `docs/`，构建维护使用 `chore/`。

### 周二至周四：开发与验证

每天开始前：

```powershell
git status
git log -3 --oneline
```

每天只完成一个可验证的小结果：

1. 读取控制行为的代码和邻近测试
2. 写一个能失败的测试或最便宜的验证
3. 做最小改动
4. 立即运行最窄测试
5. 通过后再继续下一小步

常用检查：

```powershell
$env:QT_QPA_PLATFORM = "offscreen"
python -m pytest -q
Remove-Item Env:QT_QPA_PLATFORM
python -m compileall -q .
python -m pip check
```

### 周五：Review、Retrospective、发布判断（45 分钟）

1. 按 Issue 验收标准逐项检查
2. 真机功能记录型号、ACCID、固件、工况和结果
3. 创建 PR，等待 Windows/Linux CI
4. CI 通过后 Squash Merge
5. 只写三条复盘：Keep、Stop、Try
6. 判断是否达到版本发布条件

未满足验收标准就不合并；没有平台资产或启动验证就不发布。

---

## 4. 每个需求的完整路径

```text
想法
  -> GitHub Issue（Backlog）
  -> 补齐验收标准（Ready）
  -> 选入本周（In Progress，最多 1 个）
  -> 功能分支 + 测试
  -> Pull Request（In Review）
  -> CI + 手工/真机验收
  -> Squash Merge（Done）
  -> 达到发布门槛后创建 Tag
```

### 创建 Issue 时必须写

- 谁遇到了什么问题
- 解决后有什么价值
- 可观察、可判断的验收标准
- 本次明确不做什么
- Windows / Linux / Cross-platform
- 是否涉及运动、校零、断电或真实凭据
- 故事点：1、2、3、5、8；超过 8 必须拆分

### 分支提交

```powershell
git add <本次Issue相关文件>
git diff --cached --check
git commit -m "feat: concise behavior description"
git push -u origin <分支名>
```

不要习惯性执行 `git add .`。先看 `git status`，只暂存当前 Issue 的文件。

---

## 5. 优先级怎么判断

使用下面的顺序，不按“看起来有趣”排序：

| 优先级 | 判断标准 | 示例 |
|--------|----------|------|
| P0 | 阻断安装、启动、核心验收或发布 | 自动检查不能跑、安装包不能启动、凭据泄露 |
| P1 | 明显减少售后时间或提高结果可信度 | 验收报告、历史记录、新机器人适配 |
| P2 | 改善体验但不阻断任务 | 视觉优化、快捷操作、次要统计 |

同优先级时，先做：安全风险 > 数据丢失 > 核心流程 > 高频人工操作 > 外观。

---

## 6. Definition of Done（完成定义）

一个 Issue 只有全部满足才算 Done：

- [ ] 验收标准全部满足
- [ ] 新行为有自动化测试，或明确写出不能自动化的原因
- [ ] `python -m pip check` 通过
- [ ] `python -m compileall -q .` 通过
- [ ] 目标平台 CI 通过
- [ ] 应用实际打开验证
- [ ] 用户可见变化已更新文档
- [ ] 没有密码、私钥、日志、数据库或本地配置进入 Git
- [ ] 有清楚的回滚方式

涉及真实机器人时额外要求：

- [ ] 记录型号、ACCID、固件和测试日期
- [ ] 明确地面/吊装/维修台工况
- [ ] 明确停止方式和观察人员
- [ ] 未完成的真机验证写进 PR，不能默认为通过

---

## 7. 发布版本怎么做

版本号使用 `VERSION`：

- 修 Bug：`1.0.1 -> 1.0.2`
- 向后兼容的新功能：`1.0.1 -> 1.1.0`
- 不兼容变更：`1.x -> 2.0.0`

创建 Tag 前检查：

```powershell
git switch main
git pull
python -m pip check
python -m compileall -q .
$env:QT_QPA_PLATFORM = "offscreen"
python -m pytest -q
Remove-Item Env:QT_QPA_PLATFORM
```

然后确认：

- [ ] Milestone 中所有发布阻断 Issue 已关闭
- [ ] Windows/Linux CI 通过
- [ ] 对应平台更新说明已完成
- [ ] 真机核心流程已回归
- [ ] 已确定回滚版本

满足后才执行：

```powershell
git tag v1.0.1
git push origin v1.0.1
```

Tag 会触发 GitHub Release 工作流。发布完成后必须下载产物，在目标电脑上安装并启动一次。

---

## 8. 出现问题时先做什么

### 不知道下一步做什么

1. 打开产品路线图
2. 查看最高优先级的 `status:ready` Issue
3. 没有 Ready Issue 就做 Backlog Refinement，不直接编码

### 代码改乱了

先停止继续编辑：

```powershell
git status
git diff
```

不要使用 `git reset --hard`。把输出交给协作者或 AI 助手判断哪些是你的改动。

### 测试失败

只修与当前 Issue 有关的失败。把命令、完整错误和复现步骤记录到 Issue/PR。

### 真机不在手边

完成自动测试后，把 Issue 标记为 `status:blocked`，写明所需机器人、工况和验证步骤。不要把“未验证”写成“完成”。

---

## 9. 如何与 AI 助手协作

每次给出四类信息：

1. **目标**：想让哪个用户完成什么任务
2. **现象**：现在发生了什么，最好附截图/日志
3. **成功标准**：怎样算完成
4. **限制**：平台、机器人、工况、不能做什么

推荐提示格式：

```text
目标：售后人员能导出一次自动验收报告。
现象：当前检查结束后只显示在表格里，关闭软件就丢失。
成功标准：保存机器人 ACCID、每项结果、时间和备注，可导出 CSV。
限制：本次不做飞书自动回写，不触发机器人运动，Windows/Linux 都要支持。
请先创建 Issue 和验收标准，再创建分支实现，通过 PR 合并。
```

AI 可以帮助分析、实现和验证，但产品优先级、真机安全确认和最终发布决定由你负责。

---

## 10. 每周自检

- 本周是否只承诺了一个 Sprint Goal？
- 是否有超过一个 In Progress Issue？
- 本周交付了什么可演示结果？
- 哪个步骤最浪费时间？
- 下周只改进哪一件流程问题？

对于独立维护者，稳定完成一个小目标，比同时启动五个功能更快。

---

## 11. 产品基线怎么完成

“添加一个产品”不能只增加 SN 正则。一个产品基线至少包含：

1. **官方依据**：记录文档 URL、版本、修订日期和当天是否可复核
2. **身份规则**：型号前缀、SSID、ACCID 与信息页 SN 的一致性
3. **网络拓扑**：主机、伴随/开发节点和已确认服务端点
4. **能力矩阵**：`supported`、`pending_validation`、`unsupported`
5. **独立工作区**：只显示该产品允许的页面和 SSH 入口
6. **只读验收**：没有官方期望值的项目不做硬编码 PASS/FAIL
7. **安全隔离**：未验证运动、校零和写操作不得进入工具白名单
8. **自动验证**：Profile、Workspace、验收项和隐藏入口均有测试
9. **基线文档**：列出已支持、未支持、文档风险和后续准入条件

产品基线的“完成”表示应用能够安全识别和承载该产品，并不表示全部 SDK 接口已经完成真机验收。

### 11.1 编码前 Definition of Ready

新增或修正机器人型号时，先创建“机器人产品基线”Issue，并一次性确认以下事实：

| 类别 | 必须确认的内容 | 不接受的替代方式 |
|------|----------------|------------------|
| 官方依据 | 产品名、URL、版本、修订日期、今日是否可访问 | 仅凭旧代码或记忆 |
| 正式身份 | 型号字符串、完整 ACCID/SN 精确规则、环境变量 | 从热点名或示例猜前缀 |
| 正例 | 真实完整 SN、5G/2.4G SSID、8080 SN | 只写通配符 `*` |
| 负例 | 相似非法名、非数字 SN、多余后缀、其他产品 | 只测试成功路径 |
| 旧格式 | 明确拒绝或显式兼容 | 实现时临时决定 |
| 网络拓扑 | 每个节点 role、标签、IP、用户、SSH 是否开放 | 默认复制 Oli/Luna |
| 服务矩阵 | 8080、8090、5000、MCP 等逐项证据和状态 | 端口能连就算支持 |
| 能力矩阵 | Supported / Pending validation / Unsupported | 文档有接口就开放 |
| 工具白名单 | 精确工具名；首版通常为空 | 复用 `ALL_ROBOT_TOOLS` |
| 工作区 | 默认页、显示页、隐藏页、SSH 快捷入口 | 只隐藏按钮不拦路由 |
| 验收基线 | 只读检查和每项明确期望值 | 猜 CPU、电机数或频率 |
| 安全边界 | 工况、停止方式、是否执行写操作/运动 | 以“应该安全”代替证据 |

任一项缺失时，Issue 只能处于 Refinement / Blocked，不能进入 Ready，不能修改 Profile、Workspace、Wi-Fi 正则或工具白名单。

### 11.2 减少无效往返的提问方式

AI 或协作者发现事实不足时，必须一次性列出全部缺失项，不要每轮只问一个问题。推荐直接发送：

```text
新增型号前还缺少以下事实，请一次性提供：
1. 正式产品名与官方文档版本
2. 完整 SN/ACCID 正则及至少一个真实正例
3. 5G/2.4G SSID 正例与必须拒绝的负例
4. 8080 实际返回的 SN/型号字段
5. 旧前缀是否兼容
6. 节点 IP、用户、SSH 是否开放
7. 8080/8090/5000/MCP 支持矩阵
8. 首版允许工具、工作区页面和只读验收项
9. 未验证能力和真机安全边界
```

收到完整事实包后，只读取最近的控制路径与邻近测试，先写最小身份/工作区测试，再开始实现。不要为了“多了解一点”广泛扫描整个仓库。

### 11.3 实现顺序

1. 写身份正例、负例和旧格式决策测试
2. 实现精确 ACCID 与 Wi-Fi 规则
3. 建立空工具白名单的 Profile
4. 建立只读 Workspace 和服务 UI 门控
5. 绑定共享异步连接的 generation / ACCID / 通知 SN
6. 加入只读验收；未知期望值不判 PASS/FAIL
7. 更新日期化基线文档
8. 全仓回归和双平台 CI 通过后标记 Done

### 2026-09-07 TRON2 基线完成

- Issue：#58
- 官方依据：TRON 2 SDK 开发指南 V0.2
- Profile：`tron2`
- 型号前缀：`TRON2A_*`
- 工作区：TRON2 验收、日志诊断、设置
- 只读检查：Wi-Fi、8080、`.4` 开发电脑 SSH 和系统时间
- 控制工具：空白名单
- 未开放：运动、灯效、校零、MCP、8090、`.2` SSH、硬件疲劳测试
- 基线文档：`TRON2产品基线-20260907.md`
- 真机动作：未执行

后续能力必须另建 Issue，完成专项真机验证后才能从 `pending_validation` 升级为 `supported`。