# Oli Robot Manager 单人敏捷维护手册

## 1. 这套流程解决什么问题

这份手册面向独立维护者。你同时承担产品负责人、开发者、测试人员和发布负责人，但不要在同一时刻扮演所有角色。

推荐节奏：**一周一个 Sprint，同时只开发一个 Issue（WIP = 1）**。

核心规则只有五条：

1. 想法先写 Issue，不直接改 `main`
2. Issue 先写验收标准，再开始编码
3. 一次只做一个 Issue
4. 通过测试和手工验收后才合并
5. 只有满足发布清单才创建版本 Tag

---

## 2. 你的四个角色

| 时间点 | 角色 | 只回答的问题 |
|--------|------|--------------|
| 选需求时 | Product Owner | 哪个问题最影响验收效率或软件可用性？ |
| 写代码时 | Developer | 最小改动是什么？如何自动验证？ |
| 合并前 | Reviewer / Tester | 验收标准真的满足了吗？会伤到机器人吗？ |
| 发布时 | Release Owner | 产物能安装、启动、回滚吗？ |

不要一边写代码一边扩大需求。新想法放回 Backlog，当前 Issue 不加范围。

---

## 3. 一周 Sprint 节奏

### 周一：Planning（30 分钟）

1. 查看开放 Issue
2. 只选一个 `status:ready` 的最高优先级 Issue
3. 确认验收标准、平台、安全影响和“不做什么”
4. 把它加入当前 Milestone
5. 创建分支

```powershell
git switch main
git pull
git switch -c feat/<issue编号>-<简短名称>
```

Bug 使用 `fix/`，文档或流程使用 `docs/`，构建维护使用 `chore/`。

### 周二至周四：开发与验证

每天开始前：

```powershell
git status
git log -3 --oneline
```

每天只完成一个可验证的小结果：

1. 读取控制行为的代码和邻近测试
2. 写一个能失败的测试或最便宜的验证
3. 做最小改动
4. 立即运行最窄测试
5. 通过后再继续下一小步

常用检查：

```powershell
$env:QT_QPA_PLATFORM = "offscreen"
python -m pytest -q
Remove-Item Env:QT_QPA_PLATFORM
python -m compileall -q .
python -m pip check
```

### 周五：Review、Retrospective、发布判断（45 分钟）

1. 按 Issue 验收标准逐项检查
2. 真机功能记录型号、ACCID、固件、工况和结果
3. 创建 PR，等待 Windows/Linux CI
4. CI 通过后 Squash Merge
5. 只写三条复盘：Keep、Stop、Try
6. 判断是否达到版本发布条件

未满足验收标准就不合并；没有平台资产或启动验证就不发布。

---

## 4. 每个需求的完整路径

```text
想法
  -> GitHub Issue（Backlog）
  -> 补齐验收标准（Ready）
  -> 选入本周（In Progress，最多 1 个）
  -> 功能分支 + 测试
  -> Pull Request（In Review）
  -> CI + 手工/真机验收
  -> Squash Merge（Done）
  -> 达到发布门槛后创建 Tag
```

### 创建 Issue 时必须写

- 谁遇到了什么问题
- 解决后有什么价值
- 可观察、可判断的验收标准
- 本次明确不做什么
- Windows / Linux / Cross-platform
- 是否涉及运动、校零、断电或真实凭据
- 故事点：1、2、3、5、8；超过 8 必须拆分

### 分支提交

```powershell
git add <本次Issue相关文件>
git diff --cached --check
git commit -m "feat: concise behavior description"
git push -u origin <分支名>
```

不要习惯性执行 `git add .`。先看 `git status`，只暂存当前 Issue 的文件。

---

## 5. 优先级怎么判断

使用下面的顺序，不按“看起来有趣”排序：

| 优先级 | 判断标准 | 示例 |
|--------|----------|------|
| P0 | 阻断安装、启动、核心验收或发布 | 自动检查不能跑、安装包不能启动、凭据泄露 |
| P1 | 明显减少售后时间或提高结果可信度 | 验收报告、历史记录、新机器人适配 |
| P2 | 改善体验但不阻断任务 | 视觉优化、快捷操作、次要统计 |

同优先级时，先做：安全风险 > 数据丢失 > 核心流程 > 高频人工操作 > 外观。

---

## 6. Definition of Done（完成定义）

一个 Issue 只有全部满足才算 Done：

- [ ] 验收标准全部满足
- [ ] 新行为有自动化测试，或明确写出不能自动化的原因
- [ ] `python -m pip check` 通过
- [ ] `python -m compileall -q .` 通过
- [ ] 目标平台 CI 通过
- [ ] 应用实际打开验证
- [ ] 用户可见变化已更新文档
- [ ] 没有密码、私钥、日志、数据库或本地配置进入 Git
- [ ] 有清楚的回滚方式

涉及真实机器人时额外要求：

- [ ] 记录型号、ACCID、固件和测试日期
- [ ] 明确地面/吊装/维修台工况
- [ ] 明确停止方式和观察人员
- [ ] 未完成的真机验证写进 PR，不能默认为通过

---

## 7. 发布版本怎么做

版本号使用 `VERSION`：

- 修 Bug：`1.0.1 -> 1.0.2`
- 向后兼容的新功能：`1.0.1 -> 1.1.0`
- 不兼容变更：`1.x -> 2.0.0`

创建 Tag 前检查：

```powershell
git switch main
git pull
python -m pip check
python -m compileall -q .
$env:QT_QPA_PLATFORM = "offscreen"
python -m pytest -q
Remove-Item Env:QT_QPA_PLATFORM
```

然后确认：

- [ ] Milestone 中所有发布阻断 Issue 已关闭
- [ ] Windows/Linux CI 通过
- [ ] 对应平台更新说明已完成
- [ ] 真机核心流程已回归
- [ ] 已确定回滚版本

满足后才执行：

```powershell
git tag v1.0.1
git push origin v1.0.1
```

Tag 会触发 GitHub Release 工作流。发布完成后必须下载产物，在目标电脑上安装并启动一次。

---

## 8. 出现问题时先做什么

### 不知道下一步做什么

1. 打开产品路线图
2. 查看最高优先级的 `status:ready` Issue
3. 没有 Ready Issue 就做 Backlog Refinement，不直接编码

### 代码改乱了

先停止继续编辑：

```powershell
git status
git diff
```

不要使用 `git reset --hard`。把输出交给协作者或 AI 助手判断哪些是你的改动。

### 测试失败

只修与当前 Issue 有关的失败。把命令、完整错误和复现步骤记录到 Issue/PR。

### 真机不在手边

完成自动测试后，把 Issue 标记为 `status:blocked`，写明所需机器人、工况和验证步骤。不要把“未验证”写成“完成”。

---

## 9. 如何与 AI 助手协作

每次给出四类信息：

1. **目标**：想让哪个用户完成什么任务
2. **现象**：现在发生了什么，最好附截图/日志
3. **成功标准**：怎样算完成
4. **限制**：平台、机器人、工况、不能做什么

推荐提示格式：

```text
目标：售后人员能导出一次自动验收报告。
现象：当前检查结束后只显示在表格里，关闭软件就丢失。
成功标准：保存机器人 ACCID、每项结果、时间和备注，可导出 CSV。
限制：本次不做飞书自动回写，不触发机器人运动，Windows/Linux 都要支持。
请先创建 Issue 和验收标准，再创建分支实现，通过 PR 合并。
```

AI 可以帮助分析、实现和验证，但产品优先级、真机安全确认和最终发布决定由你负责。

---

## 10. 每周自检

- 本周是否只承诺了一个 Sprint Goal？
- 是否有超过一个 In Progress Issue？
- 本周交付了什么可演示结果？
- 哪个步骤最浪费时间？
- 下周只改进哪一件流程问题？

对于独立维护者，稳定完成一个小目标，比同时启动五个功能更快。