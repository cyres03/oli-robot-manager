# Oli Robot Manager 更新说明 2026-07-07

## 1. 本次更新目标

本次更新主要解决售后验收中“舞蹈/动作库测试”的连续执行、状态恢复和问题定位需求。

重点目标：

- 修复动作库动作无法正常执行的问题
- 增加动作连续执行 5 次能力
- 优化动作完成后回到 Walk 模式的判断
- 增加窗口最小化恢复和 UI 事件日志
- 重新生成当前版本发送包

---

## 2. 功能更新

### 2.1 动作库动作执行修复

修复前现象：

- 点击动作库中的动作后，机器人只进入 Menu
- 随后自动切回 Walk
- 日志可能出现 `fail_invalid_mode`

原因：

- `request_set_motion_engine(mode=1)` 返回 `success` 后，机器人不一定已经真正进入动作库模式
- 旧逻辑收到 `success` 后立即发送 `request_execute_atomic_motion`
- 机器人尚未进入 `action_library` 时会返回 `fail_invalid_mode`

修复后逻辑：

1. 发送 `request_set_motion_engine(mode=1)`
2. 轮询 `request_get_action_library_status`
3. 等待 `action_library_mode == "action_library"`
4. 再发送 `request_execute_atomic_motion`

---

### 2.2 动作连续执行 5 次

动作页新增：

```text
5次
```

按钮位置：

```text
舞蹈动作库 -> 动作 (Motions) -> 每个动作卡片
```

执行规则：

- 连续执行 5 次
- 每次动作必须执行成功后才进入下一轮
- 每轮成功后等待 2 秒再继续
- 任意一次失败会自动中止
- 连续执行期间会锁住其它舞蹈/动作按钮，避免并发操作

---

### 2.3 停止连续动作

动作页顶部新增：

```text
停止连续动作
```

说明：

- 用于停止后续轮次
- 已经发送给机器人的当前动作不会被强制打断
- 当前动作结束后不再继续下一次

---

## 3. 稳定性修复

### 3.1 Walk 模式误报失败修复

修复前现象：

- 机器人实际已经回到 Walk
- 页面日志仍提示“自动切回拟人行走模式失败”

原因：

SDK 可能返回：

```json
{
  "result": "fail_state_not_allowed",
  "current_state": "Walk"
}
```

该返回表示机器人已经处于 Walk 状态，不是失败。

修复后：

- `fail_state_not_allowed + current_state=Walk` 按成功处理

---

### 3.2 动作失败后的恢复流程优化

修复前：

- 动作请求失败后仍继续执行退出动作库和切 Walk
- 某些情况下可能让机器人进入 `RecoveryWalk`

修复后：

- 动作未成功时不再继续执行恢复流程
- 避免错误状态连锁

---

### 3.3 窗口最小化恢复优化

修复前现象：

- 机器人执行动作/舞蹈时最小化窗口
- 再次打开时，页面可能卡住或不能正常显示到前台

修复后：

- 只在真正从最小化恢复时执行窗口恢复逻辑
- 避免任意窗口状态变化反复触发恢复流程
- 恢复时刷新主界面并尝试激活窗口

---

## 4. 日志增强

新增 UI 事件日志：

```text
C:\Users\Limx\AppData\Local\OliRobotManager\ui_events.log
```

记录内容包括：

- 窗口初始化
- 窗口最小化/恢复
- 恢复后激活状态
- 舞蹈/动作执行状态变化
- 当前页面 index
- 页面动画状态

用途：

- 排查窗口未响应
- 排查最小化恢复异常
- 判断连续动作是否按预期执行

---

## 5. 本次打包文件

当前已生成两个发送包：

### 5.1 便携版

```text
release_packages\OliRobotManager-portable-20260707-145802.zip
```

使用方式：

```text
解压后运行 OliRobotManager.exe
```

### 5.2 源码运行包

```text
release_packages\OliRobotManager-source-mainpy-20260707-145802.zip
```

使用方式：

```powershell
pip install -r requirements.txt
python main.py
```

---

## 6. 发布前验证结果

已完成以下检查：

1. 便携版构建成功
2. 便携版 exe 可启动
3. 源码包可解压
4. 源码包可正常导入 `main.py`
5. 本机新版 `main.py` 可启动
6. 相关 Python 文件编译通过
7. VS Code 诊断无错误

---

## 7. 建议验收测试

建议重点测试以下流程：

1. 连接机器人 Wi-Fi
2. 打开舞蹈动作库
3. 刷新动作列表
4. 单次执行一个动作
5. 使用 `5次` 连续执行动作
6. 连续执行中点击 `停止连续动作`
7. 动作执行期间最小化窗口，再恢复窗口
8. 查看底部日志和 `ui_events.log`

重点观察：

- 是否还出现 `fail_invalid_mode`
- 是否能正常进入动作库动作
- 每次动作完成后是否等待 2 秒再执行下一次
- 连续动作失败时是否自动中止
- 窗口恢复后是否正常显示
