# Oli Robot Manager 售后验收工作台 使用说明

## 1. 简介

Oli Robot Manager 是面向售后、测试、交付验收场景的机器人测试工作台，用于把现场常用的 Wi-Fi、HTTP、SSH、日志、校零、断电恢复等检查集中到一个 Windows 桌面软件中，减少重复打开网页、终端和外部工具的时间。

当前软件主要支持：

- 自动识别机器人 Wi-Fi 与 ACCID / SN
- 查看机器人在线状态、电量、控制器、IMU 等信息
- 执行基础姿态、模式切换、灯效、动作库和行走控制
- 一键运行售后自动验收项
- 读取 8080 机器人版本信息
- 从 8090 下载 `.log` / `.log.active` 日志并自动分析
- 执行断电恢复测试流程
- 执行 MissionEngine 完整校零
- 通过 SSH 直连执行 Backlash 测试并下载 YAML 结果
- 快速打开主控和感知机 SSH 终端

支持型号：

| 型号 | SSID 示例 | 当前能力范围 |
|------|-----------|--------------|
| Oli | `HU_D04_01_121_5G` | 现有控制、验收、日志和校零能力 |
| Luna L04 | `HU_L04_01_091_5G` | 身份、状态、日志、节点检查和只读查询 |

L04 的运动、动作执行、校零、Backlash、灯效和语音设置尚未完成专项真机验收，
软件会主动禁用这些功能，不能通过手动选择 Oli 型号绕过。

安装包下载链接：

```text
installer/Output/OliRobotManager-Setup.exe
```

---

## 2. 环境准备

### 2.1 连接机器人 Wi-Fi

1. 机器人开机完成后，使用电脑连接机器人 Wi-Fi
2. 常见 Wi-Fi 名称格式：
   - `HU_Dxx_xx_xxx`
   - `HU_Dxx_xx_xxx_5G`
   - `HU_Dxx_xx_xxx_2.4G`
   - `HU_L04_01_xxx_5G`
   - `HU_L04_01_xxx_2.4G`
   - `WF_TRON2A_xxx`
   - `WF_TRON2_xxx`
3. 默认 Wi-Fi 密码：

```text
12345678
```

### 2.2 确认机器人网络

连接机器人 Wi-Fi 后，建议先在浏览器访问：

```text
http://10.192.1.2:8080
http://10.192.1.2:8090
```

页面说明：

| 地址 | 用途 |
|------|------|
| `http://10.192.1.2:8080` | 机器人信息页，可查看 SN / ACCID 和版本信息 |
| `http://10.192.1.2:8090` | 日志与文件下载页 |
| `http://10.192.1.2:18080/mcp` | Oli MCP 服务地址；L04 显示 N/A |
| `ws://10.192.1.2:5000` | WebSocket 控制地址 |

如果 8080 页面能正常打开，软件通常可以自动识别当前机器人。

### 2.3 按型号连接信息

| 项目 | Oli | Luna L04 |
|------|-----|----------|
| 主控 SSH | `limx@10.192.1.2` | `limx@10.192.1.2` |
| 伴随节点 SSH | `guest@10.192.1.3`（感知） | `guest@10.192.1.4`（语音/视觉） |
| WebSocket | `ws://10.192.1.2:5000` | `ws://10.192.1.2:5000` |
| MCP | `http://10.192.1.2:18080/mcp` | N/A |
| 关节/电机数 | 由机器人返回 | 27 |

SSH 密码向项目负责人获取，源码仓库不保存真实密码。软件不再设置默认 ACCID；
无法确认当前机器人身份时，所有控制请求都会被拒绝。

首次连接某台机器人时，软件可能要求输入一次对应 SSH / sudo 密码。勾选
“记住到系统凭据管理器”后，密码会按机器人 ACCID、IP 和用户名隔离保存：

- Windows 使用 Windows Credential Manager
- Linux 使用桌面 Secret Service
- 密码不会写入项目目录、日志或 `config.local.json`
- SSH 公钥授权完成后，日常 SSH 操作优先使用密钥，不再使用密码
- 可在“设置 → 安全凭据”中查看状态或清除当前机器人的已保存密码

系统凭据管理器不可用时，“记住”选项会自动禁用，密码只用于当前操作。

---

## 3. 安装与启动

### 3.1 安装版使用方法

1. 下载并双击安装包：

```text
installer/Output/OliRobotManager-Setup.exe
```

2. 按安装向导完成安装
3. 可选择创建桌面快捷方式
4. 安装完成后，通过桌面快捷方式或开始菜单启动 `Oli Robot Manager`

首次运行时，Windows 可能弹出防火墙提示，请允许程序访问局域网，否则 WebSocket、MCP、SSH、8080/8090 检查可能失败。

### 3.2 便携版使用方法

如果拿到的是便携版目录：

1. 打开 `dist/OliRobotManager`
2. 双击 `OliRobotManager.exe`

### 3.3 源码运行方法

如果需要从源码运行或二次开发，需要安装 Python 依赖：

```bash
python -m pip install -r requirements.txt
python main.py
```

重新打包安装包需要额外安装：

| 依赖 | 用途 |
|------|------|
| Python 3.11+ | 运行源码和打包脚本 |
| PyInstaller | 生成 `dist/OliRobotManager` 便携版程序 |
| Inno Setup 6 | 生成 Windows 安装包 |

打包命令：

```powershell
.\build_release.ps1 -Clean
```

如果只生成便携版，不生成安装包：

```powershell
.\build_release.ps1 -Clean -SkipInstaller
```

---

## 4. 依赖与子程序说明

### 4.1 普通使用者是否需要安装依赖

安装版和便携版已经内置 Python 运行环境与主要依赖，普通使用者不需要单独安装 Python、PyQt6、Paramiko、httpx、websockets 等库。

### 4.2 源码依赖

源码运行时使用以下主要依赖：

| 依赖 | 用途 |
|------|------|
| `PyQt6` | 桌面软件界面 |
| `mcp` | MCP 服务调用 |
| `paramiko` | SSH 登录主控和感知机 |
| `websockets` | WebSocket 机器人控制 |
| `httpx` | 访问 8080/8090/MCP HTTP 地址 |
| `pytest` / `pytest-qt` / `pytest-asyncio` | 开发测试使用 |

### 4.3 随安装包携带的子程序/资源

| 文件 | 用途 | 是否需要手动下载 |
|------|------|------------------|
| `resources/backlash/backlash_install.zip` | Backlash 检测时上传到机器人主控的检测程序包 | 否，已随安装包携带 |
| `resources/logo/oli_manager_logo.ico` | 程序图标和安装包图标 | 否 |
| `resources/styles/dark_theme.qss` | 程序样式资源 | 否 |

说明：当前软件的 Backlash 功能优先使用内置直连 SSH 流程，不再依赖手动打开原始 `backlash-console-v0.5.exe` Web 页面。原始工具可作为人工备选，但不是安装版运行的必要条件。

### 4.4 本地数据保存位置

软件运行中下载或生成的数据默认保存在：

| 数据 | 保存位置 |
|------|----------|
| 8090 日志 | `%LOCALAPPDATA%\OliRobotManager\logs` |
| Backlash YAML 结果 | `%LOCALAPPDATA%\OliRobotManager\backlash\results` |
| 软件数据库/配置缓存 | `%USERPROFILE%\.oli-robot-manager` |

---

## 5. 界面说明

### 5.1 左侧导航区

左侧提供以下入口：

- 舞蹈&动作库
- 基础控制
- 验收测试
- 健康检查
- 校零
- 设置
- 快捷 SSH

左上角会显示当前 Wi-Fi 名称。识别到机器人热点时显示为绿色；连接了非机器人热点时显示为橙色；未连接时显示为红色。

### 5.2 顶部状态栏

顶部状态栏显示机器人实时状态，包括：

- 型号与 SN / ACCID
- 当前机器人状态
- 当前控制器
- 电量
- IMU 状态
- 运行模式

### 5.3 底部连接状态

底部显示以下连接项：

| 项目 | 含义 |
|------|------|
| WiFi | 当前电脑是否连接机器人热点 |
| MCP | `10.192.1.2:18080` 是否可访问 |
| WS | `10.192.1.2:5000` WebSocket 是否可用 |
| SSH | 主控 SSH 是否可登录 |

L04 不提供 MCP 服务时显示 `MCP N/A`，不计为连接失败。

### 5.5 启动与换机

- 只连接一台受支持机器人时，软件根据 WiFi SSID 与 8080 SN 自动加载 Profile。
- 同一机器人的 2.4G/5G 视为同一个实例。
- 同时连接不同机器人时弹出选择窗口；选择后只断开其他机器人 WiFi，不影响办公网络。
- SSID 与 SN 不一致、未知型号或无目标时进入安全锁定，不发送 WebSocket/SSH 控制请求。

### 5.4 日志输出区

窗口底部日志区会显示动作执行、诊断、断电、校零、Backlash 和错误信息。遇到异常时，优先查看这里的红色错误日志。

---

## 6. 功能说明

### 6.1 舞蹈&动作库

该页面用于加载并执行机器人支持的舞蹈、动作和行走控制。

主要功能：

| 功能 | 说明 |
|------|------|
| 刷新舞蹈 | 读取当前机器人支持的舞蹈列表 |
| 刷新动作 | 读取当前机器人支持的动作列表 |
| 动作库引擎 | 启动或切换动作库相关能力 |
| 行走控制 | 通过 `vx`、`vy`、`yaw` 设置行走速度 |
| 序列器 | 将多个动作按顺序组合执行 |

注意事项：

- 只有当前机器人版本支持的舞蹈和动作才会显示
- 行走控制前请确认机器人周围有足够空间
- 行走速度范围一般为 `-1.00` 到 `1.00`

### 6.2 基础控制

该页面用于快速执行常见控制指令。

主要功能：

| 分组 | 功能 |
|------|------|
| 姿态控制 | 准备站立、站立、坐下、躺下 |
| 模式切换 | 行走模式、动作库模式、阻尼模式、零力矩 |
| 测试 | 直线行走测试 |
| 状态与音频 | 查询动作库状态、唤醒词、开启/关闭唤醒 |
| 灯效 | 绿灯常亮、蓝灯呼吸、关闭灯效 |

补充说明：

- 行走模式通常需要机器人处于允许行走的状态
- 直线行走测试默认执行一小段时间后自动停止
- 不再从基础控制页触发校零，校零相关操作集中在“校零”页面

### 6.3 验收测试

该页面是售后验收的主要工作台，包含“自动验收”、“日志分析”、“断电恢复”三个标签页。

#### 自动验收

自动检查项包括：

| 模块 | 测试项 | 检查方式 |
|------|--------|----------|
| 网络 | 机器人 Wi-Fi 识别 | 本机 Wi-Fi |
| 网络 | 机器人信息页 8080 | HTTP |
| 网络 | 日志页面 8090 | HTTP |
| 网络 | MCP 服务 18080 | HTTP |
| SSH | 主控 SSH 登录 | `limx@10.192.1.2` |
| SSH | 感知 SSH 登录 | `guest@10.192.1.3` |
| 主控 | 主控系统时间 | SSH `date` |
| 主控 | 主控磁盘空间 | SSH `df -h /` |
| 主控 | 主控机器人进程 | SSH `ps` |
| 感知 | 感知 CPU 核心数 | SSH `nproc` |
| 感知 | 3D/双目相机 USB | SSH `lsusb` |
| 主控 | IMU 频率 | SSH `mrostopic hz /ImuData` |

机器人信息页 8080 需要同时满足 `HTTP 200` 和已知页面标识。当前兼容旧版 `LimX Robot Manager`、新版 `LimX Studio` 及包含 `get_robot_info` 的页面；无关设备返回的普通 200 页面仍判定为失败。验收摘要会显示实际识别到的页面类型。

按钮说明：

| 按钮 | 功能 |
|------|------|
| 运行全部自动检查 | 按顺序执行所有验收项 |
| 运行选中项 | 只执行表格中选中的检查项 |
| 刷新 8080 版本 | 从机器人信息页读取主控、分电板、主站板、驱动器版本 |
| 刷新日志列表 | 从 8090 读取 `.log` 和 `.log.active` 文件列表 |
| 下载并分析 | 下载选中的日志并自动导入日志分析器 |

#### 日志分析

日志分析支持手动导入或从 8090 下载后自动导入。

分析内容包括：

- 分电板版本
- 主站版本
- 主控软件版本
- 驱动器版本
- 当前控制器
- 控制器切换时间线
- 通讯异常
- 电机语音警告
- 驱动器上电/下电事件
- 关键词搜索和跳转

#### 断电恢复

断电恢复用于现场验证机器人断电重启后的状态一致性。

标准流程：

1. 执行断电前检查
2. 手动关闭机器人电源
3. 点击“已关机”
4. 等待 5 分钟倒计时
5. 手动重新上电
6. 程序执行重启后检查
7. 查看断电前后对比结果

### 6.4 健康检查

健康检查用于快速确认机器人基础环境是否正常。

检查内容包括：

- Wi-Fi 连接状态
- CPU 核心数
- 双目/3D 相机连接状态
- 系统时间
- IMU 频率

### 6.5 校零

校零页面包含“完整校零”和“Backlash 测试”。

#### 完整校零 MissionEngine

软件先通过 SSH 探测当前固件是否提供完整状态机接口：

```text
/mission_engine/switch_state Calibration
```

接口存在时，这条链路与遥控器 `L1 + R1` 的完整校零流程一致。软件随后关闭 SDK LED 控制，避免白灯覆盖机器人默认蓝色校零灯语；调用失败时会恢复 LED 控制。

部分固件（已确认 `robot-hu-r-2.4.10.20260814174405`）不提供该接口。此时软件会在关闭 LED 和执行校零前停止，并提示使用实体遥控器 `L1 + R1`。`/joint/calibration` 是 MissionEngine 内部步骤，软件不会绕过完整状态机直接调用。

#### Backlash 测试

Backlash 测试通过 SSH 直连机器人主控执行，默认连接：

| 项目 | 默认值 |
|------|--------|
| 机器人 IP | `10.192.1.2` |
| SSH 用户 | `limx` |
| SSH 密码 | 向项目负责人获取 |
| 机器人 ID | `HU_D04_01_121` |
| 检测轮数 | `1` |

按钮说明：

| 按钮 | 功能 |
|------|------|
| 准备环境 | 上传并安装 `backlash_install.zip`，暂停 `joint_calibration` 节点 |
| 开始检测 | 准备环境后运行 `backlash_detection --robot-id <机器人ID>` |
| 刷新状态 | 查看内置直连流程状态 |
| 下载结果 | 下载 `/home/limx/backlash/results/*.yaml` |
| 断开 | 恢复 `joint_calibration` 节点并断开 SSH |

检测完成后，结果会保存到：

```text
%LOCALAPPDATA%\OliRobotManager\backlash\results
```

安全提示：Backlash 会驱动机器人关节运动，执行前必须确认机器人固定可靠、周围无人员和障碍物。

### 6.6 设置

设置页面可修改：

- MCP 地址
- WebSocket 地址
- 机器人 ACCID
- 主控 IP 与用户名
- 感知机 IP 与用户名
- Wi-Fi SSID 前缀
- Wi-Fi 密码
- 期望 CPU 核心数

Wi-Fi SSID 前缀支持多个值，使用英文逗号分隔，例如：

```text
HU_D, WF_TRON2A, WF_TRON2, WF_
```

### 6.7 快捷 SSH

左侧底部提供两个快捷入口：

| 按钮 | 目标 |
|------|------|
| 主控 | `limx@10.192.1.2` |
| 感知 | `guest@10.192.1.3` |

点击后会打开系统终端。首次连接时可能需要确认 SSH 指纹或输入密码。

---

## 7. 典型操作流程

### 7.1 首次连接机器人

1. 电脑连接机器人 Wi-Fi
2. 打开 `Oli Robot Manager`
3. 查看左侧 Wi-Fi 状态是否识别到机器人热点
4. 查看顶部是否出现 SN、电量和机器人状态
5. 如未识别，进入“设置”手动填写 ACCID

### 7.2 执行自动验收

1. 进入“验收测试”页面
2. 点击“刷新 8080 版本”
3. 点击“运行全部自动检查”
4. 查看表格中每一项的状态和摘要
5. 如果某项失败，选中该项后点击“运行选中项”复测

### 7.3 下载并分析日志

1. 进入“验收测试”页面
2. 点击“刷新日志列表”
3. 在下拉框选择目标日志
4. 点击“下载并分析”
5. 在“日志分析”标签页查看版本、异常和事件时间线

### 7.4 执行 MissionEngine 完整校零

1. 确认机器人处于安全姿态
2. 进入“校零”页面
3. 点击“执行完整校零”
4. 如果日志提示固件未提供完整接口，改用实体遥控器 `L1 + R1`
5. 否则观察机器人是否进入校零状态和蓝色灯语
6. 查看校零日志区是否返回成功

### 7.5 执行 Backlash 测试

1. 确认机器人安全固定，周围无障碍物
2. 进入“校零”页面
3. 检查机器人 IP、SSH 用户、密码、机器人 ID 是否正确
4. 点击“准备环境”
5. 准备成功后点击“开始检测”
6. 等待检测完成并自动下载 YAML 结果
7. 如需手动再次下载，点击“下载结果”
8. 测试结束后点击“断开”恢复 `joint_calibration` 节点

### 7.6 执行断电恢复测试

1. 进入“验收测试”页面的“断电恢复”标签
2. 执行断电前检查
3. 手动关闭机器人电源
4. 点击“已关机”
5. 等待 5 分钟倒计时
6. 手动重新上电
7. 等待程序完成重启后检查
8. 查看对比结果

---

## 8. 注意事项

- 执行站立、坐下、躺下、行走、校零、Backlash 前，请确认机器人周围空间充足
- Backlash 测试会移动关节，不要在机器人未固定或有人靠近时执行
- 断电恢复测试需要人工配合，不要让程序无人值守运行
- 如 SSH 检查失败，先确认电脑仍连接机器人 Wi-Fi
- 如 8080 可打开但 WebSocket 控制失败，检查 ACCID 是否与机器人信息页一致
- 如日志很多，下载 `.log.active` 可能需要等待较长时间

---

## 9. 常见问题

**Q: 程序提示未连接机器人**

A: 先确认电脑已连接机器人 Wi-Fi，再访问 `http://10.192.1.2:8080`。如果网页打不开，说明当前网络没有连到机器人。

**Q: 8080 页面能打开，但控制按钮无响应**

A: 检查设置里的 WebSocket 地址是否为 `ws://10.192.1.2:5000`，并确认 ACCID 与 8080 页面显示的 SN 一致。

**Q: SSH 检查失败**

A: 确认主控 IP 为 `10.192.1.2`，主控用户为 `limx`；感知机 IP 为 `10.192.1.3`，用户为 `guest`。如果密码被现场修改，需要同步修改设置或输入框。

**Q: 刷新 8090 日志列表为空**

A: 先在浏览器访问 `http://10.192.1.2:8090/log/`。如果浏览器也打不开，说明 8090 服务或网络不可用。

**Q: 日志分析没有识别出版本或异常**

A: 可能所选日志不包含对应关键词。可使用搜索框查找 `pms_version`、`ecm_version`、`ability_running`、`link_status` 等字段。

**Q: MissionEngine 校零失败**

A: 查看校零日志区。若提示缺少 `/mission_engine/switch_state`，表示当前固件未开放软件完整校零接口，请使用实体遥控器 `L1 + R1`；这不是 SSH 密码错误。其他错误再检查 SSH 和机器人端 `/opt/limx/install` 环境。

**Q: Backlash 提示找不到 `backlash_install.zip`**

A: 安装版已内置该文件。如果源码运行，请确认项目中存在 `resources/backlash/backlash_install.zip`。

**Q: Backlash 检测后没有看到新 YAML**

A: 检查校零日志区是否出现检测失败或超时信息，也可以通过 SSH 查看机器人端目录 `/home/limx/backlash/results`。

---

## 10. 版本与发布信息

当前安装包版本：

```text
1.0.1
```

安装包路径：

```text
installer/Output/OliRobotManager-Setup.exe
```

便携版路径：

```text
dist/OliRobotManager/OliRobotManager.exe
```
