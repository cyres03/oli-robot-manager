# HU_L04_01 与 Oli 接口兼容性调研

## 1. 调研信息

| 项目 | 内容 |
|------|------|
| GitHub Issue | `#11` `[Spike] HU_L04_01 与 Oli 接口兼容性盘点` |
| 调研日期 | 2026-08-23 |
| Oli 基线 | `HU_D04_01_*`；现有客户端代码 + 历史审计样本 |
| 新机器人 | `HU_L04_01_091` |
| 新机器人软件 | `robot-luna-r-1.2.12.20260821201520` |
| 主控 | `limx@10.192.1.2`，RK3588/aarch64，Ubuntu 22.04.5 |
| 伴随节点 | `guest@10.192.1.4`，主机名 `aispeech`，产品 `RM2`，版本 `1.1.7.26060601` |
| 安全范围 | 被动监听、只读 HTTP、查询类 WebSocket、无 sudo 的只读 SSH |

本次没有执行运动、模式切换、阻尼、零力矩、校零、灯效、音量/唤醒设置、
机械臂、夹爪、WiFi 配置、服务重启或远端写入。

## 2. 结论摘要

不能只通过“接口名称相同”判断完整兼容。按证据层级计算：

| 层级 | 结果 | 结论 |
|------|------|------|
| WebSocket envelope | 一致 | `accid/title/timestamp/guid/data` 可复用 |
| 被动状态通知 | 一致并扩展 | `notify_robot_info` 可复用，新增 `Fall_Detection`、`owner_info` |
| Oli 请求标题静态交集 | `40/40`，100% | 40 个现有请求标题都在新机器人主控关键二进制中精确出现 |
| 只读查询握手 | `11/11`，100% | 11 项均收到同 GUID 的对应 `response_*` |
| 只读查询数据可用 | `7/11`，63.6% | 7 项 success；3 项 `fail_no_data`；1 项 `fail_disconnected` |
| 控制类行为验证 | `0/29` | 按安全边界未实际执行，不能宣称行为完全兼容 |
| 已知 HTTP/WS 入口 | `.2` 上 `3/4` | 8080、8090、5000兼容；18080 MCP 不存在 |

**推荐方案：共享现有 `RobotClient`，新增 `RobotProfile` 和薄能力适配器。**

不建议复制一套 `LunaRobotClient`；也不建议仅把 `HU_L` 加入 WiFi 前缀后直接开放全部按钮。

## 3. 身份与网络拓扑

### 3.1 新机器人身份

- WiFi SSID：`HU_L04_01_091_5G`
- ACCID / SN：`HU_L04_01_091`
- 主控软件：`robot-luna-r-1.2.12.20260821201520`
- ECM：`2.0.6`
- PMS：`1.2.3`
- 电机固件：27 个电机，版本均为 `1.1.16`
- EtherCAT：`OK`
- IMU：`OK`

现有 `extract_robot_accid()` 已能解析 `HU_L04_01_091`，但
`RobotConfig.wifi_ssid_patterns` 只有 `HU_D`、`WF_*`，因此侧边栏 WiFi 判断和周期换机识别不能完整支持 `HU_L`。

### 3.2 节点拓扑

| 角色 | Oli 当前假设 | HU_L04_01_091 实际情况 | 影响 |
|------|--------------|-------------------------|------|
| 主控 | `.2`，用户 `limx` | `.2`，用户 `limx` | 地址和账号可复用 |
| 感知节点 | `.3`，用户 `guest` | `.3` 不可达 | 固定 `.3` 检查全部失败 |
| 语音/视觉伴随节点 | 未建模 | `.4`，用户 `guest`，主机名 `aispeech` | 需要新增节点角色或替代 perception 节点 |

`.4` 不是单一语音盒，而是独立 RK3588 伴随计算节点：

- 8 CPU 核、7.7 GiB 内存
- `AudioDeviceNode`
- `speech_playback_node`
- `GestureMrosNode`
- `mroswebvideo` 和头部相机
- cosa/opus 人脸、视觉、手势进程
- 独立 `robot-app` 和 MROS 时钟

因此第一版配置可把 `.4` 设为 `companion`，并承担原 `perception` 的 CPU、时间和相机检查；代码中不应继续把角色名称硬编码成 `.3`。

## 4. 网络服务兼容性

| 服务 | Oli 预期 | HU_L04 结果 | 兼容性 |
|------|----------|-------------|--------|
| Robot Portal | `.2:8080` | 200；`get_robot_info`、`get_local_version` 可用 | 兼容 |
| 日志服务 | `.2:8090/log/` | 200；可列出 `.log` 文件 | 兼容 |
| WebSocket SDK | `.2:5000` | 可连接，持续推送 `notify_robot_info` | 兼容 |
| MCP HTTP | `.2:18080/mcp` | 端口拒绝 | 不兼容；应对该型号禁用 |
| `.4:18080` | 无 | cosa-cv 人脸库静态控制台；`/mcp` 返回 404 | 新能力，不可误当 MCP |
| `.4:18338` | 无 | cosa/opus 人脸库 HTTP API | 新能力 |
| `.4:8091` | 无 | TCP 开放，普通 HTTP 空响应 | 协议待识别 |
| `.4:50002` | 无 | TCP 开放，HTTP 无响应 | 内部服务，协议待识别 |
| `.4:59000-59006` | 无 | TCP 开放 | 内部服务，协议待识别 |

注意：当前类名 `McpWorker` 实际调用的是 WebSocket `RobotClient`，因此 `.2:18080`
缺失不会阻断主要控制链；但状态栏和自动验收中的 MCP 项会错误失败。

## 5. 被动状态通知

`notify_robot_info` 的 envelope 与 Oli 一致：

```text
accid, title, timestamp, guid, data
```

`data.result` 组别：

```text
ethercat, imu, peripheral, system_info, Fall_Detection, owner_info
```

现有 `RobotMonitor` 依赖的字段均存在：

- `peripheral.battery`
- `peripheral.bat_vol`
- `system_info.robot_status`
- `system_info.ability_running`
- `system_info.mode`
- `system_info.sn`
- `system_info.version`
- `imu.InitFail`

新增或值得纳入 Profile 的字段包括：

- `battery_capacity`
- 左右臂、左右腿分区电压/电流
- 12V/15V/24V 输出和故障
- `current_state`
- `walk_gait`
- `Fall_Detection`
- Lite LED 参数

结论：现有状态解析可继续工作，但应增加新字段映射，不需要单独重写状态传输层。

## 6. 40 个请求兼容矩阵

证据等级：

- **R-Success**：实际只读调用，收到同 GUID 的成功 response。
- **R-Recognized**：实际只读调用，收到同 GUID response，但因状态或硬件无数据未成功。
- **S-Only**：主控关键二进制存在精确请求标题；按安全边界未实际执行。

| # | Oli 请求 | 证据 | HU_L04 结果 | 结论 |
|---|----------|------|-------------|------|
| 1 | `request_action_sync` | S-Only | 精确标题存在 | 待动作工况验证 |
| 2 | `request_audio_get_wakeup_word` | R-Success | `backend=aispeech`，唤醒词可读 | 兼容 |
| 3 | `request_audio_set_volume` | S-Only | 精确标题存在 | 待非运动设置测试 |
| 4 | `request_audio_set_wakeup_word` | S-Only | 精确标题存在 | 待非运动设置测试 |
| 5 | `request_audio_wakeup_control` | S-Only | 精确标题存在 | 待非运动设置测试 |
| 6 | `request_calibrate` | S-Only | 标题存在；`joint_calibration` 进程存在 | 高风险，待专项真机验证 |
| 7 | `request_connect_wifi` | S-Only | 精确标题存在 | 待网络变更专项验证 |
| 8 | `request_damping` | S-Only | 标题存在；Damping ability 启用 | 高风险，待专项真机验证 |
| 9 | `request_dance` | S-Only | 标题存在；舞蹈列表可读 | 待安全动作测试 |
| 10 | `request_enable_led_control` | S-Only | 标题存在；状态通知含 Lite LED 字段 | 待非运动设置测试 |
| 11 | `request_enter_dance_mode` | S-Only | 标题存在；DanceLibrary ability 启用 | 待安全动作测试 |
| 12 | `request_execute_atomic_motion` | S-Only | 标题存在；动作列表可读 | 待安全动作测试 |
| 13 | `request_from_stand_to_sit` | S-Only | 标题存在；SitDown ability 启用 | 待专项真机验证 |
| 14 | `request_get_action_library_status` | R-Success | mode=`remote_control`，state=`idle` | 结构兼容 |
| 15 | `request_get_atomic_motion_list` | R-Success | 12 项，字段结构一致 | 兼容；资源集不同 |
| 16 | `request_get_claw_state` | R-Recognized | `fail_no_data` | 标题兼容，本机型可能无夹爪 |
| 17 | `request_get_dance_list` | R-Success | 6 项，字段结构一致 | 兼容；资源集不同 |
| 18 | `request_get_imu_data` | R-Success | euler[3]、quat[4]、acc[3]、gyro[3] | 兼容 |
| 19 | `request_get_joint_state` | R-Success | names/q/dq/tau 均为 27 维 | 结构兼容；维数需 Profile 化 |
| 20 | `request_get_move_pose` | R-Success | 左右 position/quaternion 字段存在，但当前全 0 | 结构兼容，语义待验证 |
| 21 | `request_get_ub_manip_ee_pose` | R-Recognized | `fail_no_data` | 标题兼容，能力待确认 |
| 22 | `request_get_wb_manip_ee_pose` | R-Recognized | `fail_no_data` | 标题兼容，能力待确认 |
| 23 | `request_led_control` | S-Only | 精确标题存在 | 待非运动设置测试 |
| 24 | `request_lie_down` | S-Only | 标题存在；StandLie/LieStand ability 启用 | 待专项真机验证 |
| 25 | `request_moveJ` | S-Only | 精确标题存在 | 参数/限位必须按 27 关节模型验证 |
| 26 | `request_moveP` | S-Only | 精确标题存在 | 待机械臂专项验证 |
| 27 | `request_prepare` | S-Only | 标题存在；IkStand ability 启用 | 待专项真机验证 |
| 28 | `request_set_claw_cmd` | S-Only | 标题存在；getter 无数据 | 默认禁用，按硬件能力开启 |
| 29 | `request_set_motion_engine` | S-Only | 标题存在；状态查询成功 | 待安全动作测试 |
| 30 | `request_set_move_mode` | S-Only | 精确标题存在 | 待机械臂专项验证 |
| 31 | `request_set_ub_manip_ee_pose` | S-Only | 标题存在；getter 无数据 | 默认禁用，待专项验证 |
| 32 | `request_set_ub_manip_mode` | S-Only | 标题存在；getter 无数据 | 默认禁用，待专项验证 |
| 33 | `request_set_walk_mode` | S-Only | 标题存在；Walk/UpdatedWalk ability 启用 | 待先验证安全停止 |
| 34 | `request_set_walk_vel` | S-Only | 精确标题存在 | 待先验证安全停止 |
| 35 | `request_set_walk_vel_sync` | S-Only | 精确标题存在 | 作为安全停止候选，需首个动作专项验证 |
| 36 | `request_set_wb_manip_ee_pose` | S-Only | 标题存在；getter 无数据 | 默认禁用，待专项验证 |
| 37 | `request_set_wb_manip_mode` | S-Only | 标题存在；getter 无数据 | 默认禁用，待专项验证 |
| 38 | `request_standup` | S-Only | 标题存在；StandUp ability 启用 | 待专项真机验证 |
| 39 | `request_wifi_connection_status` | R-Recognized | `fail_disconnected` | 协议被识别；当前未连接外部路由 |
| 40 | `request_zero_torque` | S-Only | 标题存在；ZeroTorque ability 启用 | 高风险，默认保持锁定 |

## 7. 资源集合差异

历史 `HU_D04_01_*` 审计样本通常返回：

- 舞蹈：14 项
- 原子动作：23 项

本次 `HU_L04_01_091` 返回：

- 舞蹈：6 项
- 原子动作：12 项

字段结构一致，但名称和数量不同。因此 UI 必须以服务端列表为准，不能使用 Oli 固定资源集或把缺少某个动作判定为接口不兼容。

## 8. 主控能力与硬件差异

主控为 RK3588/aarch64：

- CPU：4 核
- 内存：15 GiB
- 根分区：230 GiB
- 电机数：27
- `abilityloader` 加载 `HU_L04_01_ability.yaml`
- 存在专属 URDF、运动学、安全监控、信号和电机配置

主要启用能力：

- Damping / ZeroTorque
- IkStand / Walk / UpdatedWalk
- MotionLibrary / DanceLibrary
- StandLie / LieStand / StandUp / SitDown
- WBT、手臂、腰、头、拖动示教与遥操作

现有控制栈与 Oli 同源，但模型参数、关节数量、资源集和部分末端硬件不同。

## 9. 思必驰/伴随模块能力

`.4` 节点：

- Ubuntu 22.04.5，RK3588/aarch64
- 8 CPU 核、7.7 GiB 内存、57 GiB 根分区
- 主机名：`aispeech`
- 产品：`RM2`
- 版本：`1.1.7.26060601`

关键进程/能力：

- `AudioDeviceNode`
- `speech_playback_node`
- `GestureMrosNode`
- PipeWire / PulseAudio
- `mroswebvideo` 头部相机
- cosa/opus 人脸、视觉、手势
- `robot-app` 和 MROS 应用节点

端口语义：

- 18080：cosa-cv 人脸库静态控制台，不是 MCP
- 18338：cosa/opus 人脸库 HTTP API
- 8091、50002、59000-59006：内部服务，协议尚未识别

WebSocket `request_audio_get_wakeup_word` 通过 `.2:5000` 已成功返回
`backend=aispeech`，说明主控对 `.4` 的语音能力提供了统一代理。

## 10. 当前软件直接运行时的预期问题

1. `HU_L` 不在调研基线的 `wifi_ssid_patterns` 中，侧边栏 WiFi 和周期换机
  识别异常；已由 Bug `#12` 跟踪，完成验收前不得开始型号自动解析 Story。
2. `.3` 不存在，感知 SSH、时间、CPU、相机检查全部失败。
3. `.2:18080/mcp` 不存在，MCP 状态和验收项固定失败。
4. `.4:18080` 不能替代 MCP，它是人脸控制台。
5. 主控电机/关节为 27，不能套用固定维数。
6. 舞蹈/动作资源数量和名称不同，必须动态加载。
7. 夹爪、UB/WB 位姿返回 `fail_no_data`，相关控件应默认隐藏或禁用。
8. 校零、Backlash 和运动命令虽有静态协议证据，但尚未真机验证，不应默认开放。
9. 快捷 SSH、凭据状态和设置页仍把 companion 固定写成 `.3`。
10. `.4` 的视觉/语音能力没有独立状态和验收项。

## 11. 推荐架构方案

采用三层方案：

```text
RobotProfileResolver
  ├─ OliProfile (HU_D04_01)
  └─ LunaProfile (HU_L04_01)
          │
          ├─ shared RobotClient (现有 WebSocket 协议)
          └─ thin LunaAdapter (能力/结果归一化)
```

### 11.1 RobotProfile

建议配置字段：

```text
model_prefix
ssid_patterns
main_node {ip, user}
companion_nodes [{role, ip, user}]
service_endpoints {portal, logs, websocket, mcp, vision}
expected_motor_count
expected_cpu_cores
capabilities
acceptance_checks
```

`HU_L04_01` 初始 Profile：

- main：`.2 / limx`
- companion：`.4 / guest / speech_vision`
- MCP：unsupported
- motor_count：27
- companion_cpu_cores：8
- portal/logs/websocket：enabled
- dance/motion/joint/imu/audio-query：enabled
- claw/UB/WB：disabled 或 pending
- movement/calibration/backlash：pending_validation

### 11.2 薄适配器职责

继续复用 `RobotClient`，薄适配器只负责：

- 把 `fail_no_data` 转换为 capability unavailable，而不是通用错误。
- 归一化 27 关节模型和可选末端数据。
- 根据 Profile 暴露 UI 能力。
- 按型号提供验收项和节点拓扑。
- 扩展 `notify_robot_info` 的 `Fall_Detection` 等字段。

只有后续发现参数或状态机语义明显不同，才升级为独立协议适配器。

### 11.3 启动与换机型号解析决策

已确认需求：

> 软件启动和换机时自动根据 WiFi SSID 与机器人 SN 解析型号；仅在无目标、
> 多目标或无法识别时弹出选择窗口；用户选择机器人实例，不直接指定机器人
> 型号；识别失败不得默认使用 Oli 配置。

交互与安全规则：

1. 仅发现一台受支持机器人时，自动解析 ACCID 和型号并加载对应 Profile，
  不弹出选择窗口。
2. 同时发现多台机器人时，窗口展示 SSID、SN、自动解析的型号和连接状态，
  用户只选择目标机器人实例。
3. 未发现机器人时，弹出机器人 WiFi 选择窗口；取消后可进入离线模式，
  但所有机器人控制功能保持禁用。
4. WiFi SSID 与机器人 Portal 返回的 SN 同时可用时必须交叉校验；不一致时
  阻止控制并提示重新连接，不允许手动指定型号绕过校验。
5. 未知 SSID 前缀、未知 SN 或 Profile 缺失时显示“不支持的机器人型号”，
  不得回退到默认 ACCID 或 Oli Profile。
6. 主界面持续显示当前机器人 SN、型号和连接状态，并保留显式“切换机器人”
  入口；切换成功后重新加载 Profile、节点拓扑、能力和验收项。

## 12. 建议实现 Backlog

旧功能基线门禁：Bug `#12`“未连接时无法发现机器人 WiFi”达到 Done 后，
Story A 才能进入 Ready。不得把该缺陷隐含在型号适配实现中。

### Story A：型号配置与只读最小闭环（5 点）

- 识别 `HU_L04_01_*`
- Profile/Resolver
- 主控 `.2`、伴随 `.4`
- 状态、8080、8090、WS、SSH、时间、CPU、相机验收
- MCP 对 Luna 标为 N/A
- 不开放任何新运动能力

### Story B：能力驱动 UI（5 点）

- 动态舞蹈/动作列表
- 27 关节信息展示
- `fail_no_data` 能力降级
- 隐藏/禁用 claw、UB、WB 等未确认控件
- 扩展状态通知字段

### Story C：语音/视觉伴随节点（5 点）

- `.4` 健康状态
- AudioDevice/speech_playback/camera/vision 进程检查
- 唤醒词查询
- 人脸 API 的只读健康检查
- 明确 8091/50002/59000-59006 协议

### Story D：安全停止与基础运动（8 点）

- 单独安排地面/吊装工况
- 先验证 `request_set_walk_vel_sync` 零速度停止
- 再验证 prepare、walk mode、低速短时行走
- 超时、断连停止、操作互斥

### Story E：动作、校零与高级控制（8 点以上，继续拆分）

- 舞蹈/原子动作
- 坐下/起身/躺下
- 校零与 Backlash
- MoveJ/MoveP、UB/WB、末端硬件
- 每类能力单独真机验收

## 13. Spike 验收结论

- [x] 确认 SSID、ACCID、型号、固件和网络拓扑。
- [x] 输出 Oli 40 个请求的逐项兼容矩阵。
- [x] 真实验证 11 个只读查询接口。
- [x] 输出 `.2` 主控与 `.4` 思必驰模块能力清单。
- [x] 给出数量、比例、证据和未验证项。
- [x] 给出 Profile + 薄适配器方案。
- [ ] Product Owner Review 后拆实现 Story。

## 14. 未验证与风险声明

- 40/40 静态标题存在不等于 40/40 行为兼容。
- 29 个控制/设置请求没有执行，参数、限位、状态机和结果语义仍未知。
- `request_get_move_pose` 虽 success，但返回全 0，需要专项确认。
- claw、UB、WB 返回 `fail_no_data`，不能仅据此判断永久不支持。
- 8080 的 battery/camera 字段为 `Unknow`，应优先使用 WebSocket 状态和 companion 检查。
- 首次运动或校零测试必须重新满足 DoR，并记录工况、急停、固件和操作者。
