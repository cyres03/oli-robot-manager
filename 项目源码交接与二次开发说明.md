# Oli Robot Manager 项目源码交接与二次开发说明

- **文档用途**：帮助接手同事和其他开发者快速理解项目、运行源码，并在现有基础上适配新的机器人。
- **当前版本**：`1.0.1`
- **最后更新**：2026-08-20
- **私有 GitHub 仓库**：[cyres03/oli-robot-manager](https://github.com/cyres03/oli-robot-manager)
- **仓库负责人**：`cyres03`
- **默认分支**：`main`
- **最新迭代说明**：[更新说明-跨平台-20260820.md](更新说明-跨平台-20260820.md)
- **主要使用场景**：机器人售后、入库和交付前验收测试
**主要技术栈**：Python、PyQt6、WebSocket、HTTP、SSH、SQLite、PyInstaller、Inno Setup

---

## 1. 项目定位

Oli Robot Manager 是面向机器人验收与售后测试人员的桌面工具，目标是把以下重复工作集中到一个应用中：

- 识别并切换当前连接的机器人
- 执行舞蹈、原子动作和基础控制命令
- 检查主控、感知机、相机、IMU、时间和磁盘等状态
- 运行 WiFi、HTTP、SSH 等自动验收项
- 执行断电恢复、校零和回差检测流程
- 下载、筛选和分析机器人日志
- 保存动作执行次数、动作序列和健康检查结果

项目当前首先服务于 **Oli 机器人**。下一阶段计划在同一工具中接入另一款机器人，因此后续开发应优先抽离“机器人差异”，避免为新机器人复制整套界面和业务流程。

---

## 2. 接手前必须知道

| 项目 | 当前情况 | 处理建议 |
|------|----------|----------|
| 版本管理 | 私有仓库 `cyres03/oli-robot-manager` 使用 GitHub Flow 和轻量 Scrum | 每项开发关联 Issue，通过功能分支和 Pull Request 合并，保持 `main` 可运行 |
| 内部凭据 | SSH 首次使用时弹窗输入，可选保存到 Windows Credential Manager / Linux Secret Service | 不再要求把真实 SSH 密码写入 `config.local.json`；密码、私钥和本地配置禁止提交 |
| 大体积资源 | `backlash_install.zip` 约 113.7 MB，超过 GitHub 普通仓库单文件限制 | 不进入 Git 历史，通过私有 Release [Backlash Runtime Resource v1](https://github.com/cyres03/oli-robot-manager/releases/tag/backlash-resource-v1) 和下载脚本分发 |
| GitHub 网络 | 当前开发电脑无法连接 `github.com:443`，但可访问 GitHub API 和 `ssh.github.com:443` | 当前电脑使用 SSH 443 推送；其他开发者网络正常时优先使用 HTTPS |
| 测试条件 | 大部分控制、验收和校零功能依赖真实机器人网络 | 代码检查不能替代真机验收，涉及动作的修改必须安排安全工况测试 |
| 自动化测试 | `tests/` 已覆盖 Wayland 密码弹窗和自动验收 SSH 授权编排 | 新行为必须继续补测试；真机动作仍需手工安全验收 |
| 设置与凭据 | 连接参数仍以运行时配置为主；设置页可查看凭据后端并清除当前机器人密码 | 系统凭据按 ACCID、IP 和账号隔离，不使用 SQLite `settings` 表保存密码 |
| Linux 发布 | Linux 源码、构建脚本和 CI 已完成 | 正式 `v1.0.1` Tag 尚未创建，GitHub Release 中还没有 Linux `tar.gz` 平台资产 |
| 历史文档 | 部分文档同时包含规划设计和示例代码 | 判断当前能力时以实际源码和本文档为准 |

> 安全提示：GitHub 仓库不保存机器人密码或私钥。首次 SSH 授权通过软件密码弹窗完成；本地覆盖配置仅保留在开发者电脑，禁止上传或转发给无关人员。

---

## 3. 当前已实现功能

### 3.1 主界面

主界面由左侧导航、机器人状态区、功能页面、底部终端日志和状态栏组成。

| 页面 | 当前能力 | 主要代码 |
|------|----------|----------|
| 舞蹈&动作库 | 获取舞蹈/原子动作列表，单次执行，连续执行，停止后续轮次，动作序列和执行次数记录 | `ui/panels/dance_library_panel.py`、`services/dance_service.py` |
| 基础控制 | 行走、姿态和机器人 SDK 控制命令 | `ui/panels/control_panel.py`、`network/mcp_client.py` |
| 验收测试 | 自动检查、日志下载与分析、断电恢复 | `ui/panels/acceptance_test_panel.py` |
| 健康检查 | WiFi、CPU、相机、系统时间和 IMU 等诊断 | `ui/panels/health_check_panel.py`、`services/health_check_service.py` |
| 校零 | MissionEngine 校零、WebSocket 校零和 Backlash 回差检测 | `ui/panels/calibrate_panel.py`、`services/calibrate_service.py` |
| 设置 | 编辑运行时连接配置，查看安全凭据后端并清除当前机器人密码 | `ui/panels/settings_panel.py` |

### 3.2 自动验收项

验收页当前包含以下自动检查：

1. 机器人 WiFi 识别
2. 机器人信息页 `8080`
3. 日志页面 `8090`
4. MCP 服务 `18080`
5. 主控 SSH 登录
6. 感知机 SSH 登录
7. 主控系统时间
8. 感知机系统时间
9. 主控磁盘空间
10. 主控机器人进程
11. 感知机 CPU 核心数
12. 3D/双目相机 USB 状态
13. IMU 频率

验收页还内嵌以下标签页：

- **自动验收**：运行全部或选中检查项
- **日志分析**：获取日志列表、下载日志并分析
- **断电恢复**：由 `PowerCycleService` 驱动断电确认、倒计时和重连检查

### 3.3 机器人识别与切换

当前程序会通过以下信息识别机器人 ACCID：

1. 优先访问机器人 `8080` 信息接口读取序列号
2. 无法读取时，从当前机器人 WiFi SSID 提取 ACCID
3. 启动后每 5 秒检查机器人身份变化
4. 检测到新机器人后，更新控制目标并重新加载舞蹈和动作列表

当前支持的识别模式定义在 `config.py`，主要包括：

- `HU_*`
- `WF_TRON2A_*`
- `WF_TRON2_*`
- `WF_*`

---

## 4. 程序结构

```text
oli-robot-manager/
├─ README.md                  # GitHub 项目首页和快速开始
├─ main.py                    # 程序入口、样式加载、启动异常记录
├─ app.py                     # 组合根：创建 Worker、Service 和主窗口并完成注入
├─ config.py                  # 机器人地址、账号、阈值和本地数据目录
├─ config.example.json        # 不含真实凭据的本地配置模板
├─ GitHub协作说明.md          # 邀请、分支、提交和 Pull Request 流程
├─ database/
│  ├─ connection.py           # SQLite 连接、建表和迁移
│  └─ repository.py           # 动作次数、序列、健康结果和设置存取
├─ models/                    # 舞蹈、动作序列和健康检查数据模型
├─ network/
│  ├─ mcp_client.py           # 机器人 WebSocket SDK 请求与响应封装
│  ├─ websocket_client.py     # Qt WebSocket 客户端
│  ├─ ssh_client.py           # Paramiko SSH 封装
│  └─ wifi_manager.py         # Windows/Linux/macOS WiFi 识别与连接
├─ workers/                   # MCP、SSH、WebSocket、WiFi 后台线程
├─ services/                  # 舞蹈、健康、断电、校零、连接和状态监控业务逻辑
├─ ui/
│  ├─ main_window.py          # 页面装配、导航和全局信号连接
│  ├─ panels/                 # 各功能页面
│  ├─ widgets/                # 通用控件
│  └─ dialogs/                # WiFi 选择和 SSH 终端窗口
├─ resources/                 # 样式、Logo 和 Backlash 安装资源
│  └─ backlash/README.md      # Backlash 大资源的私有 Release 获取说明
├─ scripts/                   # Linux 环境脚本和 Backlash 资源下载脚本
├─ installer/                 # Inno Setup 安装脚本
├─ requirements.txt           # Python 依赖
└─ build_release.ps1          # Windows 发布构建脚本
```

---

## 5. 启动与调用链

```text
main.py
  └─ 创建 QApplication、加载 Logo 和 QSS
      └─ Application(app.py)
          ├─ 初始化 SQLite 数据库
          ├─ 自动识别机器人 ACCID
          ├─ 启动 McpWorker
          ├─ 启动 RobotMonitor
          ├─ 创建各 Service
          └─ 创建 MainWindow 并注入依赖
              ├─ 创建六个功能页面
              ├─ 连接 PyQt 信号槽
              └─ 触发初始 WiFi 检查
```

项目采用以下分工：

- **UI 层**：收集输入、显示状态、发出操作信号
- **Service 层**：组织业务流程和状态转换
- **Worker 层**：执行不能阻塞界面的网络或设备操作
- **Network 层**：封装具体协议、命令和响应格式
- **Model/Database 层**：保存结构化数据和本地状态

新增功能时应继续保持这个边界。不要在按钮回调中直接执行耗时 SSH、HTTP 或 WebSocket 请求。

---

## 6. 当前通信接口

| 目标 | 默认地址 | 用途 | 主要代码 |
|------|----------|------|----------|
| 机器人控制 WebSocket | `ws://10.192.1.2:5000` | SDK 命令、动作和控制请求 | `network/mcp_client.py`、`workers/mcp_worker.py` |
| MCP 服务 | `http://10.192.1.2:18080/mcp` | 服务可达性验收 | `config.py`、验收页 |
| 机器人信息页 | `http://10.192.1.2:8080` | 机器人信息、版本和序列号 | `config.py`、验收页 |
| 日志页面 | `http://10.192.1.2:8090` | 日志列表和下载 | 验收页 |
| 主控 SSH | `limx@10.192.1.2` | 主控检查、命令和文件传输 | `network/ssh_client.py` |
| 感知机 SSH | `guest@10.192.1.3` | CPU、相机和时间检查 | `network/ssh_client.py` |
| 感知机 Ollama | `http://10.192.1.3:11434/api/generate` | 预留本地模型接口 | `config.py` |

机器人控制协议是 JSON over WebSocket，核心字段包括：

```json
{
  "accid": "机器人标识",
  "title": "request_xxx",
  "timestamp": 0,
  "guid": "请求唯一标识",
  "data": {}
}
```

常见消息关系：

```text
request_xxx -> response_xxx -> notify_xxx（部分长动作）
```

修改协议映射时，要同时确认请求字段、响应结果、通知消息、超时时间和机器人状态恢复流程。

---

## 7. 本地开发环境

### 7.1 推荐环境

- Windows 10/11 或 Ubuntu 22.04 / Linux x86_64
- Python 3.10 或更高版本（CI 使用 Python 3.10）
- 可访问机器人的无线网卡
- 真机联调时连接机器人 WiFi
- Windows 安装包需要 Inno Setup 6
- Linux WiFi 识别需要 NetworkManager / `nmcli`
- Linux 安全凭据需要桌面 Secret Service；不可用时安全退化为仅本次使用

Windows 与 Linux 共用源码，但必须分别构建。GitHub Actions 已配置双平台 CI 和 Tag 发布工作流。

### 7.2 安装依赖

在项目根目录执行：

```powershell
python -m venv .venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

默认机器人地址已包含在程序配置中。首次执行 SSH 自动检查时，软件会请求一次当前机器人账号密码并安装本机 SSH 公钥；可选保存到系统凭据管理器。只有需要覆盖默认地址或兼容旧部署时才复制 `config.example.json`，真实值不得提交到 Git。

Linux 安装和源码运行：

```bash
chmod +x scripts/linux_setup.sh scripts/linux_run.sh
./scripts/linux_setup.sh
./scripts/linux_run.sh
```

如需 Backlash 回差检测，再下载私有 Release 中的运行资源：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\setup_backlash_resource.ps1
```

Release 附件 SHA-256：

```text
BACC27196221226AFE5339F3A47C9E492C565327DA0E454A7B223370E32A58EE
```

### 7.3 启动源码

```powershell
python main.py
```

启动后建议先确认：

1. 左侧 WiFi 状态是否正确
2. 当前 ACCID 是否与机器人一致
3. `8080` 信息页是否可访问
4. 底部终端是否持续输出重复错误
5. 未确认机器人安全状态前不要执行动作或校零

---

## 8. 本地数据与日志

### 8.1 SQLite 数据

默认数据库位置：

```text
%USERPROFILE%\.oli-robot-manager\oli_manager.db
```

当前主要数据表：

- `dance_counts`：按机器人 ACCID 保存舞蹈/动作执行次数
- `dance_sequences`：保存动作序列
- `health_check_results`：保存健康检查结果
- `settings`：预留的非敏感设置键值表；密码由系统凭据管理器处理

### 8.2 运行日志

```text
%LOCALAPPDATA%\OliRobotManager\crash.log
%LOCALAPPDATA%\OliRobotManager\ui_events.log
%LOCALAPPDATA%\OliRobotManager\audit\sdk_requests.jsonl
```

用途：

- `crash.log`：排查启动失败
- `ui_events.log`：排查窗口最小化、恢复和页面状态问题
- `sdk_requests.jsonl`：核对 SDK 请求、响应、通知和 ACCID

日志中禁止新增密码、令牌等敏感信息。

---

## 9. 新机器人适配策略

### 9.1 先判断协议差异

接入前先收集新机器人的以下信息：

| 维度 | 需要确认的内容 |
|------|----------------|
| 身份识别 | SSID 格式、序列号格式、型号字段、ACCID 是否存在 |
| 网络拓扑 | 主控/感知机 IP、端口、是否仍有 `8080/8090/18080/5000` |
| 控制协议 | WebSocket、MCP、HTTP 或其他 SDK；请求和响应格式是否兼容 |
| 动作能力 | 支持的行走、姿态、舞蹈、原子动作、机械臂和夹爪能力 |
| 状态数据 | 电量、运动状态、错误码和传感器字段 |
| 验收标准 | CPU 核数、IMU 频率、进程名、磁盘、相机和时间阈值 |
| 维护接口 | SSH 用户、权限、日志目录、校零和回差检测方式 |
| 安全约束 | 吊装限制、禁用动作、急停路径和状态恢复要求 |

### 9.2 同协议机器人的最小改法

如果新机器人继续使用相同 SDK 和消息结构，优先新增“机器人配置档案”，复用现有 `RobotClient` 和 UI：

1. 在 `config.py` 中增加型号、网络、账号、阈值和能力配置
2. 在 `extract_robot_accid()` 与 `WifiManager` 中增加新 SSID/序列号识别
3. 在机器人切换时加载对应配置档案
4. 将验收页中的固定 IP、命令和阈值改为读取当前档案
5. 根据能力决定是否显示舞蹈、机械臂、夹爪、校零等控件
6. 用真机验证切换前后的 ACCID、动作列表和状态监控

建议的配置概念如下，属于后续重构目标，不代表当前已有实现：

```python
@dataclass
class RobotProfile:
    model: str
    ssid_patterns: tuple[str, ...]
    main_control_ip: str
    perception_ip: str | None
    websocket_url: str
    capabilities: set[str]
    acceptance_thresholds: dict[str, float]
```

### 9.3 不同协议机器人的推荐改法

如果新机器人协议、动作名称或状态结构明显不同，不要在每个页面中增加大量 `if robot_model == ...`。建议在 Service 与 Network 之间增加机器人适配器：

```text
UI / Service
    └─ RobotAdapter（统一能力接口）
        ├─ OliRobotAdapter
        │   └─ 现有 RobotClient
        └─ NewRobotAdapter
            └─ 新机器人 SDK / HTTP / WebSocket 客户端
```

统一接口可从真实需求逐步提取，例如：

- `get_identity()`
- `get_status()`
- `list_actions()`
- `execute_action()`
- `set_walk_velocity()`
- `safe_stop()`
- `run_acceptance_check()`
- `capabilities`

不要一次性设计所有可能能力。先打通“识别 -> 状态 -> 安全停止 -> 一个低风险动作 -> 一组验收项”的最小闭环，再扩展其他功能。

### 9.4 需要重点修改的现有位置

| 文件 | 当前耦合点 | 新适配工作 |
|------|------------|------------|
| `config.py` | 固定地址、账号、SSID、阈值；密码从本地配置读取 | 引入按型号选择的配置档案 |
| `network/wifi_manager.py` | 机器人 WiFi 判断 | 支持新 SSID 与多网卡识别 |
| `network/mcp_client.py` | Oli SDK 命令和响应格式 | 同协议复用；异协议下沉到新 Adapter |
| `workers/mcp_worker.py` | 工具名到 RobotClient 的调用 | 改为面向统一适配器或能力接口 |
| `services/robot_monitor.py` | 状态消息字段解析 | 增加型号对应的状态转换 |
| `services/health_check_service.py` | 主控/感知命令与阈值 | 改为读取当前机器人验收配置 |
| `ui/panels/acceptance_test_panel.py` | 固定检查项、IP 和 SSH 命令 | 按型号生成验收项 |
| `ui/panels/control_panel.py` | 默认认为控制能力存在 | 按 `capabilities` 启用控件 |
| `services/calibrate_service.py` | Oli 校零和 Backlash 流程 | 按型号选择流程或隐藏不支持能力 |

---

## 10. 安全开发原则

机器人动作和远程命令具有真实物理风险，开发与测试时必须遵循：

1. **先确认工况**：明确机器人在地面、吊装架还是维修台
2. **先验证停止**：新适配首先验证零速度/安全停止路径，再测试其他动作
3. **低风险优先**：先状态查询，再小幅控制，最后才是完整动作
4. **不假设动作完成**：长动作同时检查 `response` 和 `notify`，并设置合理超时
5. **保持可中止**：循环和重复动作必须允许停止后续轮次
6. **防止并发命令**：动作执行期间锁定冲突操作
7. **谨慎处理状态恢复**：失败后不要盲目继续切换模式
8. **可信时间基准**：机器人时间检查使用可信网络时间，不能只与测试电脑时钟比较
9. **凭据不进日志**：SSH/WiFi 密码不得写入终端、审计日志或错误报告
10. **真机双人确认**：首次运行新动作或高风险流程时，建议一人操作、一人观察急停和环境

---

## 11. 开发与验证流程

### 11.1 多人协作初始化

仓库负责人邀请同事后，同事先克隆私有仓库：

```powershell
git clone https://github.com/cyres03/oli-robot-manager.git
cd oli-robot-manager
```

如果网络无法连接 `github.com:443`，且已经把自己的 SSH 公钥添加到 GitHub，可以使用官方 SSH 443 备用入口：

```powershell
git clone ssh://git@ssh.github.com:443/cyres03/oli-robot-manager.git
cd oli-robot-manager
```

随后按功能创建分支：

```text
feature/robot-profile
feature/<new-robot>-identity
feature/<new-robot>-adapter
feature/<new-robot>-acceptance
```

不要通过多次互传 ZIP 合并代码；ZIP 适合首次交接，不适合持续协作。
完整命令和 Pull Request 流程见 `GitHub协作说明.md`。

### 11.2 每次修改前

- [ ] 明确修改属于 UI、Service、Worker、Network 还是配置层
- [ ] 确认是否影响现有 Oli 机器人
- [ ] 记录新旧机器人的协议差异和验收标准
- [ ] 对动作类改动明确测试工况和停止方法

### 11.3 代码检查

```powershell
python -m pip check
python -m compileall -q .
$env:QT_QPA_PLATFORM = "offscreen"
python -m pytest -q
Remove-Item Env:QT_QPA_PLATFORM
```

建议优先补充以下自动化测试：

- ACCID/序列号/SSID 解析
- 机器人配置档案选择
- SDK 请求参数映射
- 响应和错误码归一化
- 状态消息解析
- 验收阈值判断
- 首次 SSH 授权、授权后重试和换机拦截
- 不支持能力时的 UI/Service 行为

### 11.4 手工回归

- [ ] 应用可以启动和退出
- [ ] 无机器人时不会持续崩溃或卡死
- [ ] 可识别并切换 Oli 机器人
- [ ] 可识别并切换新机器人
- [ ] 切换后 ACCID、状态、动作列表同步更新
- [ ] 零速度/停止命令有效
- [ ] 现有 Oli 舞蹈和动作流程无回归
- [ ] 自动验收项使用当前机器人对应的地址和阈值
- [ ] SSH、HTTP 和 WebSocket 超时不会阻塞界面
- [ ] 新克隆环境首次 SSH 检查会提示授权，成功后继续原检查
- [ ] Windows Credential Manager / Linux Secret Service 状态符合预期
- [ ] 日志不包含密码等敏感信息
- [ ] 最小化再恢复后界面仍可操作

---

## 12. 构建与发布

仅修改源码和联调时，不需要每次重新打安装包。

准备正式发布时，在项目根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build_release.ps1 -Clean
```

仅生成便携版：

```powershell
powershell -ExecutionPolicy Bypass -File .\build_release.ps1 -SkipInstaller
```

Windows 输出位置：

```text
release\windows\OliRobotManager-Windows-x64-v<version>.zip
release\windows\OliRobotManager-Windows-x64-Setup-v<version>.exe
```

Linux 构建：

```bash
./scripts/linux_setup_backlash_resource.sh
./scripts/linux_build_portable.sh
```

Linux 输出：

```text
release/linux/OliRobotManager-Linux-<arch>-v<version>.tar.gz
```

只有推送与 `VERSION` 一致的 `v<version>` Tag，GitHub Release 工作流才会生成并发布平台资产。

构建产物不应提交到源码仓库。建议忽略：

```gitignore
.venv/
__pycache__/
*.py[cod]
*.log
build/
dist/
release_packages/
installer/Output/
.claude/
.vscode/
```

---

## 13. 文档阅读顺序

| 顺序 | 文档 | 用途 |
|------|------|------|
| 1 | `项目源码交接与二次开发说明.md` | 当前架构、运行方式和新机器人适配入口 |
| 2 | `更新说明-跨平台-20260820.md` | 当前 Sprint 的自动检查与 Windows/Linux 更新状态 |
| 3 | `GitHub协作说明.md` | 私有仓库邀请、克隆、分支、提交和 Pull Request 流程 |
| 4 | `敏捷开发流程.md` | Issue、Sprint、DoR、DoD 和发布流程 |
| 5 | `更新说明-Linux-20260819.md` | Linux 源码、SSH、凭据和构建说明 |
| 6 | `使用说明-安装版.md` | 面向测试人员的安装和使用说明 |
| 7 | `SDK按钮逻辑与joystick映射说明.md` | SDK 按钮、命令和 joystick 映射 |
| 8 | `发布说明.md` | Windows/Linux 分平台构建与 Release 流程 |
| 9 | `验收测试功能改进计划.md` | 验收功能规划与优先级参考 |
| 10 | `第一阶段实现路线-代码改动指南.md` | 历史设计草案，不代表所有示例均已实现 |
| 11 | `快速参考卡.md` | 历史功能规划速查，状态需与当前源码交叉确认 |

---

## 14. 首轮新机器人适配建议

建议首轮只完成以下可验证闭环：

1. 识别新机器人型号和唯一标识
2. 正确加载该型号的地址、端口和验收阈值
3. 显示在线状态、电量和基础错误状态
4. 验证零速度或等效安全停止命令
5. 执行一个经过确认的低风险动作
6. 跑通 WiFi、HTTP/服务可达性和 SSH 基础检查
7. 保存测试结果，并确认不会污染 Oli 机器人的动作次数和配置
8. 回归现有 Oli 机器人核心流程

完成这个闭环后，再按优先级接入舞蹈库、完整基础控制、健康检查、校零、回差检测和可靠性循环。

---

## 15. 源码交接清单

- [ ] 已确认源码基线日期
- [ ] 已确认密码、私钥和 `config.local.json` 未被 Git 跟踪
- [ ] 已确认新开发者知道首次 SSH 检查会弹窗授权，不需要提交密码
- [ ] 已在公司内部 Git 仓库建立基线提交和标签
- [ ] 已分配现有 Oli 回归负责人
- [ ] 已收集新机器人 SDK、网络拓扑和验收规范
- [ ] 已明确新机器人首次动作测试的安全工况
- [ ] 已约定代码审查和真机验收流程
- [ ] 已将规划文档与当前实现状态分开管理

---

**维护说明**：每次新增机器人型号、通信协议、验收项或发布方式后，应同步更新本文档。功能状态以当前源码、真机验证记录和最新发布说明为准。