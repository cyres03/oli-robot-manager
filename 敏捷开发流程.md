# Oli Robot Manager 敏捷开发流程

## 1. 目标与原则

本项目采用轻量 Scrum + GitHub Flow，默认以两周为一个 Sprint。

核心原则：

- 每项开发必须关联 GitHub Issue。
- 小批量交付，一个 Issue 对应一个可验收结果。
- `main` 始终保持可运行，只通过 Pull Request 合并。
- 先定义验收标准，再开始编码。
- 新需求先确认受影响旧功能的现状并补齐缺陷，旧功能验收通过后再实现新增行为。
- 机器人动作相关改动以现场安全和可停止性优先。
- Windows 与 Linux 共用源码，但分别构建、验证和发布。

## 2. 角色

小团队允许一人兼任多个角色，但责任不能省略。

| 角色 | 责任 |
|------|------|
| Product Owner | 排定 Backlog、解释业务目标、接受或拒绝交付结果 |
| Developer | 拆分任务、实现、测试、提交 PR、处理评审意见 |
| Reviewer | 检查行为、回归、安全风险、凭据和发布影响 |
| Tester / Robot Operator | 在安全工况下完成真机验收并记录机器人型号、固件和结果 |

## 3. GitHub 工作项

### 3.1 工作项类型

- User Story：面向用户的可见能力。
- Bug：当前行为与预期不一致。
- Technical Task：构建、测试、架构、依赖和基础设施工作。
- Spike：有时限的调研任务，输出结论而不是产品功能。

每个工作项应标注目标平台：`Windows`、`Linux` 或 `Cross-platform`。

### 3.2 看板状态

建议在 GitHub Project 中使用以下状态：

1. Backlog：已记录，尚未承诺进入 Sprint。
2. Ready：满足 Definition of Ready，可进入 Sprint。
3. In Progress：正在实现，每位开发者同时不超过两项。
4. In Review：已有 PR，等待代码或真机评审。
5. Blocked：被权限、硬件、网络或外部依赖阻塞。
6. Done：满足 Definition of Done，已合并并完成验收记录。

## 4. Definition of Ready

Issue 同时满足以下条件才可进入 Sprint：

- 用户或业务问题清楚。
- 验收标准可观察、可判定。
- 目标平台明确。
- 依赖、机器人型号和所需资源已识别。
- 已列出受影响的旧功能，并记录当前行为、验证方式和基线结果。
- 影响范围内的旧功能已达到原验收标准；未达到时先关联并完成 Bug Issue。
- 运动或校零功能说明安全工况、停止方式和风险。
- 工作量不超过一个 Sprint；超过时先拆分。

### 4.1 旧功能基线门禁

每个 User Story 在进入 Ready 前依次完成：

1. 列出新需求将复用、修改或依赖的现有入口、服务和用户流程。
2. 按当前需求和真实使用场景复现旧功能，记录正常、异常及跨平台结果。
3. 为缺少的关键行为补自动化测试；无法自动化时写明手工验收步骤。
4. 发现旧功能缺陷时先创建 Bug Issue，修复并完成回归，不把修补工作隐含在新功能中。
5. Product Owner 确认旧功能基线后，新需求才能进入 Ready 和开始实现。

紧急安全修复可以直接进入 Bug 流程，但仍必须完成最小回归并补记基线结果。

## 5. Sprint 节奏

默认 Sprint 为两周，可根据现场节奏调整，但调整必须在 Sprint 开始前确定。

### Sprint Planning（60 分钟）

- 回顾 Product Goal 和上个 Sprint 未完成项。
- 从 Ready 中选择本期目标。
- 每项给出故事点：1、2、3、5、8；超过 8 必须拆分。
- 确定 Sprint Goal 和平台发布目标。

### Daily Stand-up（每天 10-15 分钟）

每人只回答：

- 昨天完成了什么可验证结果。
- 今天准备完成什么。
- 当前阻塞是什么，需要谁协助。

### Backlog Refinement（每周 30 分钟）

- 澄清未来一至两个 Sprint 的 Issue。
- 补齐验收标准、平台和安全影响。
- 拆分过大工作项，删除失效需求。

### Sprint Review（45 分钟）

- 演示已达到 Done 的功能。
- 按 Issue 验收标准逐项确认。
- 真机功能记录机器人 ACCID/型号、固件、工况和结果。
- 未达到标准的项目返回 Backlog，不按完成计算。

### Retrospective（30 分钟）

只保留三类结论：

- Keep：继续保持的一项做法。
- Stop：停止的一项低效做法。
- Try：下个 Sprint 尝试的一项改进，并指定负责人。

## 6. 开发流程

1. 新需求先执行旧功能基线门禁，阻断缺陷转为独立 Bug Issue。
2. 从 Ready Issue 创建分支：`feat/<issue>-name`、`fix/<issue>-name` 或 `chore/<issue>-name`。
3. 开发前同步 `main`。
4. 先完成最小可验证改动，再扩展相邻能力。
5. 每次改动后执行最窄验证，提交前执行完整验证。
6. 推送分支并创建 PR，PR 必须关联 Issue。
7. Reviewer 根据验收标准、旧功能回归、风险和安全清单评审。
8. 通过后使用 Squash and merge 合并。
9. 合并后删除远端功能分支，并同步本地 `main`。

## 7. Definition of Done

所有项目：

- Issue 验收标准全部满足。
- PR 已评审，无未解决阻断意见。
- Windows/Linux CI 按目标平台通过。
- `python -m pip check` 通过。
- `python -m compileall -q .` 通过。
- 新增行为有自动化测试；无法自动化时写明手工步骤和结果。
- 受影响旧功能的基线用例和回归测试全部通过。
- 没有密码、令牌、私钥、机器人日志或本地配置进入 Git。
- 用户可见行为和发布方式变化已更新文档。

机器人动作、校零和底层控制额外要求：

- 明确目标型号、ACCID、固件和关节/电机数量。
- 有停止方式、超时和断连保护。
- 在安全场地完成人工监护测试。
- PR 记录真机工况与结果；未做真机测试时明确标记为待验收。

发布额外要求：

- Windows 和 Linux 分别构建、命名和校验产物。
- 更新对应平台的更新说明。
- 记录版本、架构、校验值和回滚方式。

## 8. 分支、提交与 PR

分支示例：

```text
feat/123-linux-release
fix/145-perception-time
chore/160-ci-matrix
```

提交采用 Conventional Commits：

```text
feat: add Linux release package
fix: reject stale robot SSH target
chore: add cross-platform CI
```

一个 PR 应尽量只解决一个 Issue。禁止在功能 PR 中夹带无关重构。

## 9. 发布策略

- 版本号使用 `VERSION` 文件中的语义化版本。
- Git Tag 使用 `v<version>`，例如 `v1.1.0`。
- Windows 资产放在 `release/windows/`。
- Linux 资产放在 `release/linux/`。
- 两个平台分别维护更新说明和验收结果。
- 紧急修复使用 `hotfix/<issue>-name`，仍需 PR 和最小回归，不直接修改 `main`。

## 10. 建议指标

每个 Sprint 只跟踪少量可行动指标：

- Sprint Goal 是否达成。
- 完成故事点与结转故事点。
- 从 In Progress 到 Done 的周期时间。
- 线上/现场逃逸缺陷数。
- 因需求不清导致的返工项数。

指标用于改进流程，不用于个人排名。
