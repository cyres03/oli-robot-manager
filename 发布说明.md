# Oli Robot Manager 发布说明

## 1. 目标

本项目支持两种交付形式：

- 便携版：直接解压运行
- 安装版：通过安装向导安装到其它 Windows 电脑

---

## 2. 构建前提

### 2.1 必需项

- Windows
- Python 环境
- PyInstaller

### 2.2 可选项

- Inno Setup 6

说明：

- 没有 Inno Setup 6 时，仍可正常生成便携版
- 安装版依赖 Inno Setup 6 编译 `iss` 脚本

---

## 3. 构建命令

在项目根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build_release.ps1 -Clean
```

仅生成便携版：

```powershell
powershell -ExecutionPolicy Bypass -File .\build_release.ps1 -SkipInstaller
```

---

## 4. 输出目录

### 4.1 便携版

输出位置：

```text
dist\OliRobotManager
```

主程序：

```text
dist\OliRobotManager\OliRobotManager.exe
```

### 4.2 安装版

如果本机安装了 Inno Setup 6，则会额外生成：

```text
installer\Output\OliRobotManager-Setup.exe
```

---

## 5. 发布前检查

建议每次发版前至少检查：

1. 便携版 exe 能否启动
2. 侧边栏 logo 是否正常显示
3. 舞蹈页、基础控制页能否正常打开
4. 单网卡环境下是否能识别 `WF_TRON2A_*` 机器人
5. 设置页保存 Wi-Fi 前缀后是否仍可启动

---

## 6. 当前发布配置说明

当前发布链已包含以下资源：

- 全局样式表
- SVG logo
- ICO 静态图标资源

当前安装脚本文件：

```text
installer\OliRobotManager.iss
```

当前便携版构建脚本：

```text
build_release.ps1
```
