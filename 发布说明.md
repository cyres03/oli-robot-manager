# Oli Robot Manager 发布说明

## 1. 发布原则

Windows 与 Linux 共用同一套源码和版本号，但分别构建、验证和发布。不同平台的
PyInstaller 产物不能互换，也不能在一个平台交叉生成另一个平台的可执行文件。

版本号统一读取项目根目录的 `VERSION` 文件。

## 2. 发布产物

### Windows x64

| 类型 | 文件 |
|------|------|
| 便携版 | `release/windows/OliRobotManager-Windows-x64-v<version>.zip` |
| 安装版 | `release/windows/OliRobotManager-Windows-x64-Setup-v<version>.exe` |

便携版主程序位于 ZIP 内的：

```text
OliRobotManager/OliRobotManager.exe
```

### Linux

| 架构 | 文件 |
|------|------|
| x86_64 | `release/linux/OliRobotManager-Linux-x86_64-v<version>.tar.gz` |
| arm64（手工构建） | `release/linux/OliRobotManager-Linux-arm64-v<version>.tar.gz` |

主程序位于压缩包内的：

```text
OliRobotManager/OliRobotManager
```

Linux 包应在与目标机器兼容的 Linux 发行版和架构上构建。

## 3. Windows 构建

### 前提

- Windows x64
- Python 3.10+
- PyInstaller
- Inno Setup 6（仅安装版需要）

### 命令

生成便携版和安装版：

```powershell
powershell -ExecutionPolicy Bypass -File .\build_release.ps1 -Clean
```

仅生成便携版：

```powershell
powershell -ExecutionPolicy Bypass -File .\build_release.ps1 -Clean -SkipInstaller
```

中间产物仅写入：

```text
build/windows/
dist/windows/
```

## 4. Linux 构建

### 前提

```bash
sudo apt update
sudo apt install -y python3 python3-venv python3-pip network-manager openssh-client \
	libxcb-cursor0 libxcb-xinerama0 libxkbcommon-x11-0
```

### 命令

```bash
chmod +x scripts/linux_setup.sh scripts/linux_build_portable.sh \
	scripts/linux_setup_backlash_resource.sh
./scripts/linux_setup_backlash_resource.sh
./scripts/linux_setup.sh
./scripts/linux_build_portable.sh
```

中间产物仅写入：

```text
build/linux/
dist/linux/
```

## 5. GitHub Release 约定

同一版本使用一个 Tag，例如 `v1.0.1`，并上传对应平台资产：

```text
OliRobotManager-Windows-x64-v1.0.1.zip
OliRobotManager-Windows-x64-Setup-v1.0.1.exe
OliRobotManager-Linux-x86_64-v1.0.1.tar.gz
OliRobotManager-Linux-arm64-v1.0.1.tar.gz
```

不使用 `OliRobotManager.zip`、`latest.zip` 等无法判断平台和版本的名称。

将与 `VERSION` 一致的 Tag 推送到 GitHub 后，`.github/workflows/release.yml`
会自动构建 Windows x64 和 Linux x86_64，并把产物上传到同一个 Release。
Linux arm64 当前需要在 arm64 Linux 构建机上手工执行发布脚本后上传：

```bash
git tag v1.0.1
git push origin v1.0.1
```

Tag 与 `VERSION` 不一致时工作流会停止，不发布资产。

## 6. 发布前检查

所有平台：

1. `python -m pip check`
2. `python -m compileall -q .`
3. 对应平台 CI 通过
4. 产物可解压并启动
5. 未包含 `config.local.json`、密码、私钥、日志或本地数据库
6. 更新对应平台的更新说明

Windows：

1. 便携版 EXE 可启动
2. 安装、覆盖安装和卸载正常
3. Windows Credential Manager 可用
4. 图标和快捷方式正常

Linux：

1. 可执行文件权限正确
2. Wayland/X11 均完成启动检查
3. NetworkManager / `nmcli` 可识别双 WiFi 适配器
4. Secret Service 可用；不可用时能安全退化为仅本次使用
5. 原生 SSH 终端可打开

## 7. 更新说明

- Windows 历史更新：[更新说明-20260707.md](更新说明-20260707.md)
- Linux 当前更新：[更新说明-Linux-20260819.md](更新说明-Linux-20260819.md)
