# 遥操作研发提测测试用例（2026-08-24）

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 提测批次 | 2026-08-24 |
| 目标版本 | 24.5.x（以研发实际交付版本为准） |
| 测试对象 | Oli 人形机器人遥操作系统、LimX Teleop App、PICO VR 客户端 |
| 测试阶段 | 研发测试 / 真机验收 |
| 需求来源 | 飞书《Oli 遥操作及数采系统需求》8.24 提测分组 |
| 文档状态 | 草案，待产品与研发补齐量化阈值后进入 Ready |

## 2. 测试设计规则

### 2.1 需求与用例的区别

- **需求**：说明需要解决的问题和期望能力，不写实现方式。
- **验收标准**：使用可观察、可量化、可判定的语言定义通过条件。
- **测试方法**：说明采用功能验证、故障注入、数据测量、日志分析或回归对比等手段。
- **测试场景**：说明在什么模式、网络、姿态、操作顺序和异常条件下执行。
- **测试用例**：把前四项落实为可重复执行的前置条件、步骤、预期结果和证据。

### 2.2 用例最小字段

| 字段 | 填写要求 |
|------|----------|
| 用例 ID | 使用 `TC-824-需求号-序号`，可反查原始需求 |
| 标题 | 一个动作和一个可判定结果 |
| 优先级 | P0：安全/主流程；P1：核心异常与恢复；P2：兼容和体验 |
| 类型 | 基线、功能、异常、边界、恢复、性能、兼容或回归 |
| 前置条件 | 版本、机器人、工况、网络、模式、设备和初始姿态 |
| 测试步骤 | 可由另一名测试人员无歧义复现 |
| 预期结果 | 每一步均有明确结果，不使用“正常”“稳定”等模糊表述 |
| 证据 | Bag、日志、录屏、配置快照、统计结果或照片 |
| 安全与停止 | 监护人、急停方式、超限或失控时的停止条件 |

## 3. 提测准入门禁

提测前必须满足：

- [ ] 研发提供唯一版本号、Git 提交或构建时间，不使用“最新包”作为版本标识。
- [ ] 产品与研发确认本文所有“待确认”阈值和按键映射。
- [ ] 列出每项需求受影响的旧功能，并在上一稳定版本执行基线用例。
- [ ] 基线缺陷已单独登记 Bug，不混入新需求的验收结果。
- [ ] 提供升级步骤、回滚包和回滚方法。
- [ ] 明确支持的机器人型号、ACCID、PICO 型号、网络拓扑和外设组合。
- [ ] 运动测试已明确地面/维修架工况、监护人、急停和最小安全距离。
- [ ] 遥操作核心话题可以记录，机器人与 PICO/上位机时钟可关联。

## 4. 公共测试环境与证据

### 4.1 环境记录

每轮执行前记录：

| 字段 | 实测值 |
|------|--------|
| 机器人型号 / ACCID | |
| 主控软件完整版本 | |
| LimX Teleop App 版本 | |
| PICO 型号 / 系统 / App 版本 | |
| 遥操作模式 | 部分动捕 / VR 全身动捕 q-based / 其他 |
| 网络拓扑 | 本地热点 / 路由器 / 远程网络 |
| RTT / 丢包 / 抖动 | |
| 测试工况 | 地面 / 维修架 / 桌面交互 |
| 监护人 / 急停方式 | |
| 测试日期和执行人 | |

### 4.2 公共 Bag 证据集

核心链路建议记录：

```bash
mrosrecord --topic "/vr_cmd /tf /tf_static /teleop/intent_frame /teleop/session_state /teleop/scheduler_follow_state /teleop_cmd /teleop_cmd_WBT /teleop_debug_status /teleop_ub_state /teleop_wb_state /upper_body/servo/cmd /arm_status /joint/cmd /joint/state /motor/state /joystick /sdk_cmd_vel /cmd_vel /full_cmd_vel /wbc_cmd /wbc_switch_ready /mrosout /diagnostics_value /monitor" -O "TC_ID_$(date +%Y%m%d_%H%M%S)" --lz4 -b 256
```

说明：`pose_ctrl_mode`、`scheduler_follow_enabled`、pipeline 切换等是服务调用，不会直接写入 topic bag。涉及这些操作时必须同时保留界面录屏、操作时间点和 `/teleop/session_state` 结果。

## 5. 需求清单与追踪

| 需求 ID | 8.24 提测需求 | 验收状态 | 对应用例 |
|---------|---------------|----------|----------|
| RQ-824-01 | 遥操作跟随稳定性：断网、视野遮挡、丢包、网络卡顿 | 待补齐阈值 | TC-824-01-01～06 |
| RQ-824-02 | 端到端视频延迟与帧率测试（现状约 200 ms 以上） | 待确认目标值 | TC-824-02-01～04 |
| RQ-824-03 | LimX Teleop App 提供三种手势选择并记忆选择 | 待确认持久化边界 | TC-824-03-01～05 |
| RQ-824-04 | VR 左右摇杆按键可进入阻尼状态 | 待确认按键和触发规则 | TC-824-04-01～05 |
| RQ-824-05 | 模式键及 InitPose/APose 切换键变更；VR 全身动捕默认进入 q-based | 待确认最终映射 | TC-824-05-01～06 |
| RQ-824-06 | Teleop APose 与 Teleop InitPose 互切轨迹考虑桌面障碍 | 待确认桌面模型和间隙 | TC-824-06-01～05 |
| RQ-824-07 | 操作者在允许范围内任意姿态可切入全身动捕 | “任意位置”范围待定义 | TC-824-07-01～06 |

### 5.1 提测前待签字参数

以下内容未确认前可以开展探索性测试，但不能给出正式 PASS 结论：

| 所属需求 | 待确认项 | 建议输出形式 | 确认方 |
|----------|----------|--------------|--------|
| RQ-824-01 | 正常跟随持续时间 `T_baseline` | 分钟 | 产品 / 测试 |
| RQ-824-01 | 输入超时或断网后的安全响应时间 `T_safe` | 毫秒及起止事件定义 | 产品 / 研发 |
| RQ-824-01 | 网络恢复时间 `T_recover`、是否允许自动恢复 | 毫秒；自动 / 人工确认 | 产品 / 研发 |
| RQ-824-01 | 恢复时允许的最大位姿跳变 `D_jump` | 关节角和末端位姿阈值 | 算法 / 控制 |
| RQ-824-01 | 支持的丢包、延迟、抖动范围及跟随误差 | 网络档位和各档通过阈值 | 产品 / 算法 |
| RQ-824-02 | 视频延迟目标 | P50、P95、P99、最大值，单位 ms | 产品 / 视频研发 |
| RQ-824-02 | 有效帧率、丢帧和最长冻结 `T_freeze` | FPS、百分比、毫秒 | 产品 / 视频研发 |
| RQ-824-02 | 网络恢复后视频恢复时间 `T_video_recover` | 毫秒及“恢复”判定口径 | 视频研发 |
| RQ-824-03 | 三种手势的名称、顺序和默认值 | 签字版 UI / 交互定义 | 产品 |
| RQ-824-03 | 左右手是否独立，退出、App/机器人重启、升级后的记忆规则 | 生命周期状态表 | 产品 / 研发 |
| RQ-824-04 | 左右摇杆具体按键、单击或长按规则、阻尼响应 `T_damping` | 按键图和毫秒阈值 | 产品 / 控制 |
| RQ-824-05 | 模式键、InitPose/APose 键及旧按键处理方式 | 签字版完整映射表 | 产品 / VR 研发 |
| RQ-824-05 | “默认 q-based”的生命周期边界 | 首次安装 / 每次进入 / 重启状态表 | 产品 / 研发 |
| RQ-824-06 | 支持的桌高、桌深、桌沿距离和初始姿态范围 | 带坐标系和公差的尺寸图 | 机械 / 产品 |
| RQ-824-06 | 最小安全间隙 `D_clearance`、切换时间 `T_pose_switch` | 毫米、秒及测量方法 | 机械 / 控制 |
| RQ-824-07 | 允许的平移、朝向、身高/姿态和追踪质量范围 | 带坐标系的三维包络和边界值 | 产品 / VR 算法 |
| RQ-824-07 | 进入时间 `T_enter`、成功率、关节跳变 `D_joint_jump`、末端跳变 `D_ee_jump` | 秒、百分比、rad、mm/deg | 算法 / 控制 |

## 6. 详细需求与测试用例

### 6.0 公共执行约定

1. 同一用例先在上一稳定版本执行旧功能基线，再在提测版本执行；使用相同机器人、PICO、网络和初始姿态。
2. 每个动态用例执行前静置并记录 3 秒，操作完成后继续记录至少 3 秒；证据文件以用例 ID 命名。
3. 首轮运动测试在维修架或安全工况低速执行，通过后才转地面；监护人全程可触发硬件急停。
4. 出现非预期快速运动、持续抖动、失联后仍运动、关节接近限位、碰撞风险或状态不可确认时，立即停止用例。
5. 文中 `T_safe`、`T_recover`、`D_jump`、`D_clearance` 等参数必须由产品和研发在提测前给出数值；未确认时相应用例结果只能记为 `BLOCKED`，不能凭主观感受判为通过。
6. 未特别说明时，每项操作重复 3 次；断连恢复及连续切换类用例按表中次数执行。

### 6.1 RQ-824-01 遥操作跟随稳定性

**需求**：在 VR 遥操作期间发生视野遮挡、网络延迟、抖动、丢包或断网时，机器人进入规定的安全状态；故障恢复后不执行过期指令，遥操作链路可按规定恢复。

**验收标准**：

- 正常网络下连续遥操作不少于 `T_baseline`，进程不退出、会话状态不误切换、肢体无非操作者输入导致的跳变。
- 输入超时或断网后，在 `T_safe` 内停止接收旧运动意图并进入产品定义的安全状态。
- 断网期间不得继续执行断网前缓存的轨迹或速度指令。
- 恢复网络后按产品定义执行“人工确认恢复”或“自动恢复”；在 `T_recover` 内状态一致，首次输出不超过 `D_jump`。
- 单侧手柄或身体追踪丢失时，只允许执行产品明确规定的降级动作，其他肢体不得被异常带动。
- 在约定网络劣化矩阵下，故障率、跟随误差和状态切换结果满足研发提供的阈值。

**测试方法**：正常功能测试、遮挡故障注入、网络故障注入、长稳测试、断连恢复测试、Bag/日志时间线分析。

**场景矩阵**：

| 场景 | 建议参数 | 观察重点 |
|------|----------|----------|
| 正常网络 | 基线 RTT、0% 人工丢包 | 跟随连续性、误切状态、进程健康 |
| 视野遮挡 | 左手、右手、头显、身体追踪分别遮挡 1/5/30 秒 | 丢失检测、受影响肢体、恢复跳变 |
| 丢包 | 1%、5%、10%，持续 60 秒 | 输入超时、指令断续、状态提示 |
| 延迟 | 单向增加 50/100/200 ms | 跟随延迟、缓存指令、恢复行为 |
| 抖动 | 20/50/100 ms | 突跳、振荡、超时误判 |
| 断网 | 1/5/30 秒及持续断网 | 安全状态、旧指令、恢复策略 |
| 重复故障 | 断网/恢复 20 次 | 资源泄漏、状态残留、恢复一致性 |

| 用例 ID | 优先级 / 类型 | 前置条件与场景 | 测试步骤 | 预期结果与证据 |
|---------|---------------|----------------|----------|----------------|
| TC-824-01-01 | P0 / 基线、回归 | 正常网络；部分动捕与 VR 全身动捕分别执行 | 1. 记录进程、会话和网络基线。2. 连续完成双臂、身体和行走操作。3. 持续 `T_baseline`。 | 无崩溃、误切换、非输入运动或持续抖动；记录 Bag、应用日志、进程状态和录屏。 |
| TC-824-01-02 | P0 / 异常、恢复 | 维修架；分别遮挡左手、右手、头显和身体追踪 | 1. 保持低速跟随。2. 按矩阵遮挡。3. 遮挡期间改变被遮挡设备姿态。4. 恢复追踪。 | 在规定时间识别丢失；机器人按定义保持/退出跟随；恢复时不追赶遮挡期间动作，跳变量不超过 `D_jump`。 |
| TC-824-01-03 | P0 / 异常、安全 | 维修架；机器人正在低速运动 | 1. 开始跟随。2. 断开 VR/机器人数据链路 1、5、30 秒。3. 断网期间持续改变手柄和身体姿态。 | `T_safe` 内进入安全状态；断网期间无持续旧指令；UI、会话状态和机器人实态一致。 |
| TC-824-01-04 | P0 / 恢复 | 承接 TC-824-01-03 | 1. 恢复网络但不操作。2. 观察是否自动恢复。3. 按规定执行人工确认。4. 缓慢重新输入。 | 恢复策略与产品定义一致；不自动执行断网期间输入；`T_recover` 内恢复，首次输出无超限跳变。 |
| TC-824-01-05 | P1 / 边界、性能 | 维修架；使用网络故障注入工具 | 1. 逐组施加丢包、延迟和抖动。2. 每组保持 60 秒并完成固定动作。3. 每组恢复正常网络 30 秒。 | 每组均可得到跟随误差、超时次数和恢复时间；无失控或进程退出；结果逐组对比基线并保留网络参数快照。 |
| TC-824-01-06 | P1 / 可靠性 | 选择已通过的短时断网参数 | 1. 连续执行断网/恢复 20 次。2. 每次恢复后完成一次模式进入和退出。3. 比较首轮与末轮资源和时延。 | 20 次状态结果一致；无句柄/内存持续增长、重复订阅、无法重入或陈旧状态；保存统计表和异常日志。 |

### 6.2 RQ-824-02 视频端到端延迟与帧率

**需求**：量化 VR 端看到的机器人视频相对真实场景的端到端延迟、帧率和卡顿，并验证其满足研发确认的性能目标。

**验收标准**：

- 在标准网络下，端到端延迟的 P50、P95、P99 和最大值分别不超过约定阈值。
- 有效帧率的 P5 和平均值不低于约定阈值，连续冻结时长不超过 `T_freeze`。
- 运行 30 分钟无黑屏、花屏、时间戳倒退、持续降帧或客户端崩溃。
- 网络短时劣化并恢复后，在 `T_video_recover` 内恢复到基线性能，且不持续播放陈旧画面。
- 每项结论至少包含样本数、测试设备、网络条件和测量误差，不以一次肉眼观察下结论。

**测试方法**：高帧率相机同屏拍摄、毫秒计时器/二维码时间戳对比、逐帧统计、网络故障注入、长稳测试。

**场景矩阵**：标准网络、轻度丢包、100 ms 附加延迟、网络中断后恢复；静态画面和快速手部/头部运动分别测量。

| 用例 ID | 优先级 / 类型 | 前置条件与场景 | 测试步骤 | 预期结果与证据 |
|---------|---------------|----------------|----------|----------------|
| TC-824-02-01 | P0 / 性能基线 | 标准网络；真实场景与 VR 画面可被同一高帧率相机拍到 | 1. 显示毫秒计时器或可逐帧识别事件。2. 录制不少于 100 个事件。3. 逐帧计算差值。 | 输出样本数、P50/P95/P99/最大延迟及测量误差；所有指标满足目标值；保留原始视频和统计表。 |
| TC-824-02-02 | P1 / 帧率、卡顿 | 标准网络；静态和快速运动各 5 分钟 | 1. 采集显示帧时间。2. 统计有效帧率、重复帧、丢帧和最长冻结。 | 帧率及冻结指标满足阈值；没有时间戳倒退、花屏或黑屏；保留逐帧结果。 |
| TC-824-02-03 | P1 / 边界、恢复 | 施加约定丢包和延迟 | 1. 正常运行 60 秒。2. 注入网络劣化 60 秒。3. 恢复网络并继续 120 秒。 | 劣化期间行为符合降级定义；恢复后 `T_video_recover` 内回到基线，不快速补播陈旧帧。 |
| TC-824-02-04 | P1 / 可靠性 | 标准网络；设备电量和温度满足要求 | 1. 连续显示视频 30 分钟。2. 每 5 分钟执行固定快速运动。3. 比较各时间段统计值。 | 无崩溃或持续性能退化；首末 5 分钟指标差异在约定范围；记录温度、资源、网络和视频。 |

### 6.3 RQ-824-03 三种手势选择与记忆

**需求**：LimX Teleop App 提供三种规定手势；选择后立即用于对应手部，并在退出/重入遥操作及规定的重启边界后保留上次选择。

**验收标准**：

- 三种手势名称、图标、顺序和默认值与产品定义一致，不多不少。
- 每种手势选择后，UI 选中态、下发命令和机器人实际手势一致。
- 左右手是否独立选择与保存，必须与产品定义一致。
- 退出再进入遥操作、重启 App、重启机器人后的保留行为分别符合产品定义。
- 配置缺失、损坏或版本升级时回退到明确默认值，不崩溃、不下发越界手指命令。

**测试方法**：UI 功能测试、命令与实态对比、持久化测试、重启恢复测试、异常配置测试。

**场景矩阵**：三种手势逐个选择；左/右手组合；退出重入；App 重启；机器人重启；升级或配置异常。

| 用例 ID | 优先级 / 类型 | 前置条件与场景 | 测试步骤 | 预期结果与证据 |
|---------|---------------|----------------|----------|----------------|
| TC-824-03-01 | P0 / 功能 | 首次安装或清除测试配置 | 1. 打开手势设置。2. 核对三种选项和默认值。3. 逐项选择。 | 选项、顺序和默认值正确；单选状态唯一；无不可用或重复选项；保存 UI 截图。 |
| TC-824-03-02 | P0 / 功能、数据 | 机器人手部处于安全展开位置 | 1. 左右手分别选择三种手势。2. 逐次触发开合。3. 对比命令与实态。 | 每次均执行所选手势；左右手行为符合独立/联动定义；命令维度、范围正确；保存 Bag 和录屏。 |
| TC-824-03-03 | P1 / 持久化 | 已选择非默认手势 | 1. 退出遥操作。2. 重新进入。3. 再次触发手势。 | UI 与实际动作均保持上次选择；不得仅 UI 记住而控制端回到默认值。 |
| TC-824-03-04 | P1 / 恢复 | 已选择非默认手势 | 1. 重启 Teleop App 后检查。2. 重启机器人后检查。3. App 与机器人同时重启后检查。 | 三个边界的行为均与产品定义一致；若某边界不要求持久化，应明确恢复默认值并保持 UI/实态一致。 |
| TC-824-03-05 | P1 / 异常、兼容 | 可使用测试配置或升级路径 | 1. 模拟配置缺失、非法索引或旧版本值。2. 启动 App 并进入遥操作。 | 不崩溃、不发送越界命令；回退到约定默认值并给出可诊断记录；升级后合法旧值按兼容策略处理。 |

### 6.4 RQ-824-04 VR 摇杆按键进入阻尼

**需求**：在支持的遥操作状态下，按下规定的 VR 左摇杆或右摇杆按键可使机器人进入阻尼状态，并且该安全操作不与其他按键功能冲突。

**验收标准**：

- 左摇杆按键和右摇杆按键均可独立触发阻尼，触发方式与产品定义一致。
- 从有效按键事件到机器人进入阻尼的时间不超过 `T_damping`。
- 进入阻尼后停止遥操作运动输出，UI、会话状态和机器人实际状态一致。
- 单击、长按、连续按和左右同时按不会触发重复切换、崩溃或其他动作。
- 在部分动捕、全身动捕、机器人运动中和网络异常时，阻尼行为符合安全策略。

**测试方法**：功能测试、按键边界测试、运动中安全测试、网络异常测试、输入事件与状态时间线分析。

**场景矩阵**：左键、右键、双键；单击、长按、连击；部分动捕、全身动捕；静止、运动；正常网络、卡顿、断网。

| 用例 ID | 优先级 / 类型 | 前置条件与场景 | 测试步骤 | 预期结果与证据 |
|---------|---------------|----------------|----------|----------------|
| TC-824-04-01 | P0 / 功能、安全 | 维修架；部分动捕；机器人静止 | 1. 进入遥操作。2. 单击左摇杆按键。3. 复位后单击右摇杆按键。 | 两个按键均在 `T_damping` 内独立触发阻尼；UI、会话和实态一致；记录输入和状态时间差。 |
| TC-824-04-02 | P0 / 功能、安全 | 维修架；VR 全身动捕 q-based | 重复 TC-824-04-01。 | q-based 模式下结果一致；退出全身控制输出，无残留 `/teleop_cmd_WBT` 运动。 |
| TC-824-04-03 | P0 / 动态安全 | 维修架低速运动，通过后地面低速 | 1. 执行双臂或身体跟随。2. 运动中分别按左右摇杆键。 | 在 `T_damping` 内停止运动并进入阻尼；无突然加速、反向或继续执行缓存轨迹。 |
| TC-824-04-04 | P1 / 边界 | 静止、安全工况 | 1. 分别长按、快速连按 5 次、左右同时按。2. 每种重复 3 轮。 | 一次操作只产生一次有效状态转换；无模式抖动、重复弹窗、崩溃或误触其他功能。 |
| TC-824-04-05 | P0 / 异常、安全 | 维修架；分别注入卡顿和持续断网 | 1. 卡顿时按阻尼键。2. 断网前后分别按键。3. 观察本地与机器人安全状态。 | 正常可达链路按 `T_damping` 生效；不可达时客户端明确提示，机器人由断连保护进入安全状态；不得显示“已阻尼”但机器人仍运动。 |

### 6.5 RQ-824-05 按键映射变更与默认 q-based

**需求**：按新版定义调整模式切换键和 InitPose/APose 切换键；进入 VR 全身动捕时默认选择 q-based AvatarTeleop 控制链路。

**验收标准**：

- 新按键映射与产品签字版本完全一致，帮助文本、UI 提示和实际行为一致。
- 旧按键不再触发已迁移功能，且不得与阻尼、手势、行走等功能冲突。
- 每次从非全身动捕状态进入 VR 全身动捕时，实际控制模式为 q-based；UI 标签不能代替实际状态证据。
- 模式切换及 InitPose/APose 切换一次按键只执行一次，失败时保持原安全状态并说明原因。
- App/机器人重启、退出重入和连续切换后，默认与持久化规则一致。

**测试方法**：按键映射功能测试、输入事件与实际控制状态对比、冷启动测试、连续切换、冲突与回归测试。

**场景矩阵**：首次启动、退出重入、App 重启、机器人重启；新旧按键；单击/长按/连击；部分动捕与全身动捕双向切换。

| 用例 ID | 优先级 / 类型 | 前置条件与场景 | 测试步骤 | 预期结果与证据 |
|---------|---------------|----------------|----------|----------------|
| TC-824-05-01 | P0 / 功能 | 新版按键映射已由产品签字 | 1. 逐个触发新版模式键和姿态键。2. 对比 UI、输入事件、会话状态及实际动作。 | 每个新键只触发定义功能；显示和实际状态一致；保留映射清单、录屏和 Bag。 |
| TC-824-05-02 | P0 / 回归、冲突 | 同上 | 1. 逐个按旧按键。2. 组合测试阻尼、手势、行走和新按键。 | 旧键不再触发迁移功能；新映射不误触其他功能；产品保留的旧功能仍正常。 |
| TC-824-05-03 | P0 / 默认值 | 从非全身动捕状态开始；机器人安全站立 | 1. 进入 VR 全身动捕。2. 不额外按模式键。3. 检查会话 mode、q-based 输出和实际控制器。 | 默认进入 q-based AvatarTeleop；对应全身输出有效；不先进入其他模式再自动跳转。 |
| TC-824-05-04 | P0 / 状态切换 | 已进入 q-based | 1. 按新模式键切到规定的另一模式。2. 再切回 q-based。3. 重复 10 次。 | 每次只转换一次；状态机、UI、输出话题和实际控制器一致；无卡死、并行控制或残留输出。 |
| TC-824-05-05 | P0 / 姿态切换 | 维修架或无桌面障碍环境 | 1. 处于 InitPose。2. 按新版姿态键切到 APose。3. 再切回 InitPose。 | 两个方向均执行正确轨迹；最终姿态、状态和提示一致；没有错误进入模式切换或阻尼。 |
| TC-824-05-06 | P1 / 恢复、边界 | 已切到非默认模式和非默认姿态 | 1. 退出重入。2. 重启 App。3. 重启机器人。4. 对模式键和姿态键快速连按。 | 每个生命周期边界遵循签字规则；连按被去抖或串行处理；不出现状态与实态分离。 |

### 6.6 RQ-824-06 APose 与 InitPose 互切避让桌面

**需求**：在产品声明的桌面尺寸和机器人相对位置范围内，Teleop APose 与 Teleop InitPose 双向切换轨迹不与桌面碰撞，并可被安全中止。

**验收标准**：

- 产品必须给出桌面高度、深度、机器人到桌沿距离及允许初始姿态范围，不能用“常规桌面”代替。
- 在声明范围内，机器人任一部位与桌面的动态最小间隙不小于 `D_clearance`。
- 轨迹无关节限位、速度/加速度超限、明显停顿、反复规划或突跳。
- 两个方向均能在 `T_pose_switch` 内完成，并到达定义的最终姿态容差。
- 切换过程中触发阻尼/急停可在规定时间内终止，恢复时不自动续跑剩余轨迹。
- 超出声明桌面包络或初始姿态范围时，系统按定义拒绝或安全降级，不得承诺未知障碍自动避让。

**测试方法**：轨迹功能测试、桌面包络边界测试、最小间隙测量、关节数据分析、中断恢复与越界测试。

**场景矩阵**：标称桌面；高度、桌沿距离的上下边界；InitPose→APose、APose→InitPose；轻微偏离初始姿态；切换中阻尼；超出设计包络。

| 用例 ID | 优先级 / 类型 | 前置条件与场景 | 测试步骤 | 预期结果与证据 |
|---------|---------------|----------------|----------|----------------|
| TC-824-06-01 | P0 / 功能、安全 | 标称桌面与标称机器人位置；InitPose | 1. 标记桌面和机器人位置。2. 切换到 APose。3. 测量全过程最小间隙和关节数据。 | 无接触；间隙不小于 `D_clearance`；时间、终态和关节约束满足标准；保留多角度视频和 Bag。 |
| TC-824-06-02 | P0 / 功能、安全 | 标称桌面；APose | 按 TC-824-06-01 方法切换到 InitPose。 | 反向轨迹同样满足间隙、时间、终态和关节约束；不能只复用正向测试结论。 |
| TC-824-06-03 | P1 / 边界 | 桌高和桌沿距离取产品范围四角及标称点 | 1. 每个边界点双向切换 3 次。2. 记录最小间隙。 | 所有声明范围内组合通过；最差点仍满足 `D_clearance`；输出边界矩阵和最差证据。 |
| TC-824-06-04 | P0 / 中断、恢复 | 维修架或具备防碰措施 | 1. 双向切换分别在前段、中段、后段触发阻尼/急停。2. 解除后观察。 | 在规定时间停止；解除后不自动续跑；必须由明确操作重新进入安全姿态。 |
| TC-824-06-05 | P1 / 异常 | 超出桌面包络或初始姿态容差，使用无碰撞风险布置 | 1. 设置越界条件。2. 请求姿态切换。 | 系统按设计拒绝或执行已定义安全策略并提示原因；不得无提示执行可能碰撞的标称轨迹。 |

### 6.7 RQ-824-07 允许范围内任意姿态切入全身动捕

**需求**：操作者位于产品声明的追踪空间和姿态范围内时，可从不同站位、朝向和身体姿态安全切入 VR 全身动捕；切入不会使机器人产生超限跳变。

**验收标准**：

- 产品以坐标系明确平移、朝向、身高/姿态和追踪质量的有效范围，不能以“任意位置”作为验收边界。
- 有效范围内各代表点均可在 `T_enter` 内进入全身动捕，成功率达到约定值。
- 切入瞬间各关节目标和实态跳变不超过 `D_joint_jump`，末端位姿跳变不超过 `D_ee_jump`。
- 切入后参考系、左右手、朝向和缩放正确，不出现镜像、反向、整体漂移或持续重锚。
- 边界外或追踪质量不足时拒绝切入并说明原因，机器人保持原安全状态。
- 连续退出/切入后不累积坐标偏移，不执行退出期间的操作者动作。

**测试方法**：空间网格覆盖、姿态组合测试、边界值测试、追踪缺失故障注入、重复重入和 Bag 跳变量分析。

**场景矩阵**：中心和空间边界点；朝向边界；直立、屈膝、双臂不同位置；单个追踪点缺失；范围外；退出/切入 20 次。

| 用例 ID | 优先级 / 类型 | 前置条件与场景 | 测试步骤 | 预期结果与证据 |
|---------|---------------|----------------|----------|----------------|
| TC-824-07-01 | P0 / 功能基线 | 操作者位于标称中心，直立自然姿态 | 1. 从非全身动捕状态进入 q-based。2. 保持 3 秒。3. 缓慢完成规定动作。 | `T_enter` 内成功；无超限跳变；坐标、左右手和朝向正确；保存切入前后 Bag 与录屏。 |
| TC-824-07-02 | P0 / 空间覆盖 | 按允许平移范围建立中心、各边界和角点网格 | 1. 操作者逐点站位。2. 每点退出并重新切入 3 次。3. 完成同一校验动作。 | 声明范围内成功率满足目标；无位置相关镜像、漂移或跳变；输出点位热力表。 |
| TC-824-07-03 | P1 / 姿态覆盖 | 标称站位；使用产品允许姿态集 | 1. 分别以直立、屈膝、双臂前伸/侧展和左右不对称姿态切入。2. 每种重复 3 次。 | 允许姿态均可安全切入；映射正确且跳变在阈值内；不允许姿态有明确结果。 |
| TC-824-07-04 | P1 / 朝向边界 | 标称站位；测试朝向中心和上下边界 | 1. 在各朝向切入。2. 前进、转身和抬手验证方向。 | 有效范围内机器人方向与操作者一致；不出现轴交换、反向或重锚后持续偏航。 |
| TC-824-07-05 | P0 / 异常、安全 | 维修架；设置范围外站位或遮挡必要追踪点 | 1. 请求进入全身动捕。2. 恢复有效追踪后再次请求。 | 无效条件下拒绝进入并说明具体原因，机器人保持安全状态；恢复后可按正常流程进入。 |
| TC-824-07-06 | P1 / 可靠性、恢复 | 选择已通过的标称点和边界点 | 1. 交替退出/切入 20 次。2. 退出期间改变操作者姿态。3. 比较每轮锚点和跳变量。 | 不执行退出期间动作；20 次无失败或坐标漂移累积；锚点、耗时和跳变量满足阈值。 |

## 7. 执行结果汇总

| 用例 ID | 结果 | 缺陷 ID | Bag / 日志 / 录屏 | 执行人 | 日期 |
|---------|------|---------|--------------------|--------|------|
| | PASS / FAIL / BLOCKED / NOT RUN | | | | |

结果定义：

- `PASS`：全部预期结果满足且证据完整。
- `FAIL`：至少一项预期结果不满足，已登记缺陷。
- `BLOCKED`：因环境、依赖或提测准入信息缺失无法执行，不记为通过。
- `NOT RUN`：尚未执行。

## 8. 测试完成标准

- [ ] 7 项需求均有需求到用例、用例到证据的双向追踪。
- [ ] 所有 P0、P1 用例已执行，P0 通过率 100%，无未关闭安全缺陷。
- [ ] 受影响旧功能回归通过，不把“新功能通过”替代旧功能回归。
- [ ] 网络故障、断连恢复、重复按键和异常退出均有结果。
- [ ] 真机运动用例记录工况、监护人和停止方式。
- [ ] 性能结论同时报告样本数、P50、P95、P99、最大值，不只报告平均值。
- [ ] Bag、日志、录屏按用例 ID 命名，能定位到操作时间点。
- [ ] 遗留风险、未测组合和阻塞项已在提测报告中明确列出。