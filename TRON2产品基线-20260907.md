# TRON2 产品基线（2026-09-07）

## 1. 基线结论

- 产品：TRON2 EDU
- Robot Manager Profile：`tron2`
- 识别格式：`TRON2A_<数字>`，Wi-Fi 可带 `_5G` 或 `_2.4G` 后缀
- 基线状态：已建立
- 基线日期：2026-09-07
- 安全级别：只读基线；运动、校零、灯效和底层控制均未开放

本基线的目标是让 Robot Manager 能正确识别 TRON2、进入独立工作区并完成官方文档能够支持的只读连接检查。它不代表 TRON2 的所有 SDK 接口已经完成真机验收。

## 2. 依据

官方来源：

- [TRON 2 SDK 开发指南](https://www.limxdynamics.com/zh/documents/847884267345285120)
- 文档版本：V0.2
- 适用产品：TRON2 EDU
- 已核验修订日期：2026-07-21

2026-09-07 访问官方地址时页面被第三方跳转拦截，无法重新读取正文。本次实现依据此前已核验并留存的官方 V0.2 内容；官网恢复后必须重新比对版本、网络拓扑、接口字段和安全限制。若官网发布新版本，以新版本重新评审，不自动沿用本基线结论。

## 3. 官方网络与运行环境

| 对象 | 地址/标识 | 基线用途 |
|------|-----------|----------|
| 机器人热点 | `TRON2A_*` | 自动识别 Profile 与 ACCID |
| 机器人主机 | `10.192.1.2` | 8080 信息页、5000 WebSocket |
| 机器人信息页 | `http://10.192.1.2:8080` | 身份、版本和状态只读检查 |
| 上层 WebSocket | `ws://10.192.1.2:5000` | 状态通道已建模，控制未开放 |
| 开发扩展电脑 | `guest@10.192.1.4` | SSH、系统时间只读检查 |

官方开发扩展电脑环境：Ubuntu 22.04.6 LTS、ROS2 Humble、ROS1 Noetic。默认热点或 SSH 密码不得写入源码、测试、日志、诊断包或本文档，由系统凭据管理器或现场人员提供。

以下地址或服务没有进入本次 TRON2 基线：

- `10.192.1.2` SSH：官方指南未作为本基线开发入口确认
- `http://10.192.1.2:8090`：未在指南中确认
- `http://10.192.1.2:18080/mcp`：未在指南中确认

## 4. SDK 架构事实

官方指南描述两层接口：

1. 底层运动控制 SDK
   - C++ / Python
   - 支持 ROS1、ROS2 和非 ROS 使用方式
   - 直接发布关节目标，属于高风险实时控制面
2. 上层应用 WebSocket
   - 地址 `ws://10.192.1.2:5000`
   - JSON 全双工协议
   - 请求、响应和异步通知共享统一消息封装

统一 WebSocket 消息字段：

```text
accid, title, timestamp, guid, data
```

- 请求标题：`request_*`
- 同步响应：`response_*`，应返回同一 `guid`
- 异步通知：`notify_*`
- 非法请求：`notify_invalid_request`

Robot Manager 本基线只保存端点和身份上下文，不通过工具白名单发送 TRON2 控制请求。

## 5. 底层 SDK 参考边界

文档中的底层 SDK 能力包括：

- 初始化机器人连接
- 查询电机数量
- 订阅 IMU
- 订阅机器人关节状态
- 发布机器人关节命令
- 订阅手柄和诊断信息
- 设置灯效

关键安全要求：

- 仿真连接地址为 `127.0.0.1`
- 真机连接地址为 `10.192.1.2`
- 机型来自 SN 前缀，完整 SN 作为 ACCID
- 发布关节命令前必须读取实际电机数量和关节名称
- 文档示例存在 10、14、16 维描述混用，不能硬编码示例维数
- 实时关节控制必须使用稳定频率、限位检查和断连停止策略

因此，Robot Manager 将 `low_level_sdk` 标记为 `pending_validation`，本基线不包含底层 SDK 执行入口。

## 6. 上层接口参考边界

指南列出的通用能力包括：

- Wi-Fi 连接与状态查询
- 灯效控制
- 紧急停止
- 机器人信息通知

TRON2 轮足相关能力包括站立、行走、速度控制、机身高度、坐下、楼梯模式和摔倒恢复。接口存在不等于已经完成产品验收，尤其需要核验：

- 当前固件支持范围
- 机器人当前状态和命令互斥
- 速度单位与取值范围
- 连续发送频率
- 断连后的停止行为
- 紧急停止仅在“空闲模式”可调用的文档语义

因此，Robot Manager 将 `movement` 和 `led_control` 标记为 `pending_validation`，其余控制类能力保持 `unsupported`，工具白名单为空。

## 7. Robot Manager 支持矩阵

| 能力 | 状态 | 说明 |
|------|------|------|
| `TRON2A_*` 身份识别 | Supported | Wi-Fi 与 8080 SN 必须一致 |
| 独立 TRON2 工作区 | Supported | 默认进入 TRON2 验收 |
| 8080 信息页 | Supported | 只读 |
| WebSocket 端点 | Supported | 状态通道建模；无控制工具 |
| `.4` 开发电脑 SSH | Supported | `guest@10.192.1.4`，只读检查 |
| Profile 管理的网络设置 | Supported | 设置页只读，不能覆盖为其他拓扑 |
| `.4` 系统时间检查 | Supported | 不自动校时 |
| 8090 日志服务 | Unsupported | 控件禁用 |
| MCP | Unsupported | 不探测、不调用 |
| 舞蹈/动作库 | Unsupported | 不显示入口、不请求资源 |
| 基础运动控制 | Pending validation | 不显示入口、不允许调用 |
| 底层关节控制 | Pending validation | 不提供 GUI 入口 |
| 校零 / Backlash | Unsupported | 不显示入口 |
| 灵巧手疲劳测试 | Unsupported | 不提供用例 |
| 灯效 | Pending validation | 未开放 |

## 8. TRON2 工作区

TRON2 工作区只提供：

- `TRON2 验收`
- `日志诊断`（本地文件分析能力；不代表支持 8090）
- `设置`
- `开发扩展电脑 (guest@10.192.1.4)` SSH 快捷入口

不会显示：

- 舞蹈与动作库
- 基础控制
- 测试用例
- 健康检查
- 校零与 Backlash
- `10.192.1.2` SSH 快捷入口

## 9. 只读验收基线

TRON2 自动验收项：

| 检查项 | 方法 | 写机器人 |
|--------|------|----------|
| 机器人 Wi-Fi 识别 | 本机网络查询 | 否 |
| 8080 信息页 | HTTP GET | 否 |
| `.4` 开发电脑 SSH | `hostname; uname -a` | 否 |
| `.4` 系统时间 | `date` / `timedatectl show` | 否 |

TRON2 一键诊断在上述检查前增加 8080 身份、版本和状态快照；不追加 `.2 mrosservice list`。CPU 核心数没有官方验收期望值，因此不做 PASS/FAIL 判定。

## 10. 已知文档风险

下列信息必须在后续专项验证中处理：

- 底层 RobotCmd 示例的电机数量和数组维数存在冲突
- 上层与底层灯效编号存在 `+1` 偏移
- 示例 ACCID 混用不同产品前缀
- 部分 JSON 示例含缺失括号、注释或中文标点，不能直接作为协议载荷
- 轮足 `request_twist` 的 yaw 单位描述需要真机确认
- 紧急停止的可调用状态描述反常，需要专项验证

## 11. 后续准入条件

任何 TRON2 控制能力从 `pending_validation` 升级到 `supported` 前，必须：

1. 使用实际 SN 和固件建立独立 Issue
2. 记录电机数量、关节名称、单位和限位
3. 定义安全工况、观察人员和停止方式
4. 先做读取或最小幅度测试
5. 覆盖超时、断连、换机和迟到响应
6. Linux / Windows CI 通过
7. PR 中记录真机结果，不把未验证写成通过

## 12. 今日完成记录

2026-09-07 完成：

- TRON2 Profile 与 `TRON2A_*` 正式 SN 前缀识别
- TRON2 独立工作区
- 只读验收与诊断基线
- 未支持服务的 UI 门控
- 隐藏资源请求与 SSH 入口隔离
- Profile、Workspace、验收和侧栏自动化测试

本次没有执行 TRON2 运动、灯效、校零、底层关节命令或任何写操作。

### 身份纠错

2026-09-07 现场确认 TRON2 正式身份前缀为 `TRON2A_*`。基线初版误写的 `WF_TRON2_*` / `WF_TRON2A_*` 已从 Profile、Wi-Fi 检测、示例和当前文档中移除；旧前缀不会再被解析为 TRON2 Profile。
