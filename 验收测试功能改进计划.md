# Oli Robot Manager 验收测试功能改进计划

**目标**：基于售后验收实际需求，强化重复测试、可靠性测试的易用性与安全性。

**背景**：当前验收测试页（`acceptance_test_panel.py`）已支持 WiFi/HTTP/SSH 自动检查，需补充：
- 重复动作循环的精细化控制（安全间隔、暂停/停止、进度追踪）
- 吊装/风险工况的动作保护拦截
- 可靠性测试链路的人工干预机制
- 测试结果记录与报告生成

---

## 第一阶段：验收功能核心加强（1-2 周）

### 1.1 增强"断电恢复"循环控制

**当前状态**：
- [PowerCyclePanel](ui/panels/power_cycle_panel.py) 支持基础循环
- 缺少循环暂停、停止、安全间隔调节

**改进需求**：
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 可调安全间隔 | 循环中两次断电间隔秒数可调（默认 15s）| P0 |
| 暂停/恢复 | 运行中可暂停，恢复时继续当前循环 | P0 |
| 即时停止 | 任何时刻可中断整个循环 | P0 |
| 进度展示 | 实时显示已执行/总数，当前耗时 | P0 |
| 断电前警告 | 5s 倒计时提示，允许取消本次循环 | P1 |

**实现方向**：
```python
# ui/panels/power_cycle_panel.py 扩展
class PowerCyclePanel(QWidget):
    # 新增控件
    interval_spin = QSpinBox(min=5, max=120, value=15)  # 秒
    pause_btn = QPushButton("暂停")  # 显示运行时，隐藏就绪时
    stop_btn = QPushButton("停止")
    progress_label = QLabel("0 / 10")  # 当前 / 总数
    
    # 新增状态枚举
    class LoopState(Enum):
        IDLE = "就绪"
        RUNNING = "运行中"
        PAUSED = "已暂停"
        STOPPING = "停止中"
    
    def run_cycle(self):
        """循环执行断电→等待→检查→间隔 -> 暂停/停止检查"""
        
    def pause_cycle(self):
        """暂停当前循环，保留进度"""
        
    def resume_cycle(self):
        """继续运行"""
        
    def stop_cycle(self):
        """停止并清空进度"""
```

**测试验证**：
- [ ] 循环中途能否正常暂停、恢复
- [ ] 停止后进度是否重置
- [ ] 安全间隔是否被准确执行（用日志记录时间戳）

---

### 1.2 吊装工况动作保护拦截

**当前状态**：
- 存在舞蹈库与控制面板，缺少高风险工况识别
- 未做动作合法性检查

**改进需求**：
| 场景 | 保护措施 | 优先级 |
|------|--------|--------|
| 吊装状态下执行动作 | 弹窗确认，允许取消 | P0 |
| 特殊动作黑名单 | 部分舞蹈/高风险动作禁用 | P1 |
| 过程中紧急停止 | 红色按钮，任何时刻可中断 | P0 |
| 工况预设保存 | "吊装验收"、"地面验收" 预设 | P1 |

**实现方向**：
```python
# config.py 新增
RISKY_ACTIONS = {
    "high_lift": {"names": ["jump", "spin"], "severity": "danger"},
    "hoisting": {"names": ["back_flip", "side_roll"], "severity": "danger"},
}

# ui/panels/control_panel.py 和 dance_library_panel.py 扩展
@dataclass
class RobotState:
    is_hoisting: bool = False  # 当前是否在吊装状态
    is_running_action: bool = False
    last_action: str = ""

def execute_action(action_name: str, context: RobotState):
    """执行前检查工况与黑名单"""
    if context.is_hoisting and action_name in RISKY_ACTIONS["hoisting"]["names"]:
        reply = QMessageBox.warning(
            self, "吊装保护",
            f"动作 '{action_name}' 在吊装状态下风险较高，确认继续？",
            QMessageBox.StandardButton.Yes | QMessageBox.StandardButton.No
        )
        if reply != QMessageBox.StandardButton.Yes:
            return
    # 继续执行...
```

**测试验证**：
- [ ] 吊装状态下能否正确拦截高风险动作
- [ ] 可否手动切换"吊装"/"地面"预设
- [ ] 紧急停止按钮按压 <100ms 应立即生效

---

### 1.3 验收检查结果记录与导出

**当前状态**：
- 自动验收检查在表格里显示，支持日志下载
- 缺少结构化报告与结果导出

**改进需求**：
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 测试会话管理 | 每个检查周期自动生成会话 ID（时间戳） | P0 |
| 结果持久化 | 检查结果保存到本地 JSON（$HOME/.../results/） | P0 |
| 导出 CSV / 飞书表 | 支持导出到飞书验收表格式 | P1 |
| 对比历史 | 同一机器多次检查结果可对比 | P1 |

**实现方向**：
```python
# models/acceptance_result.py 新增
@dataclass
class AcceptanceCheckResult:
    check_key: str  # e.g., "wifi", "main_ssh"
    category: str  # e.g., "网络"
    name: str
    status: str  # "pass", "fail", "skip"
    detail: str  # 返回的具体信息
    timestamp: str  # ISO 8601
    duration_ms: int

@dataclass
class AcceptanceSession:
    session_id: str  # YYYYMMDD_HHMMSS_<robot_model>
    robot_model: str
    started_at: str
    completed_at: str | None
    results: list[AcceptanceCheckResult]
    
    def export_to_dict(self) -> dict:
        """导出为可上报飞书的格式"""

# ui/panels/acceptance_test_panel.py 扩展
class AcceptanceTestPanel(QWidget):
    def __init__(self, ...):
        self.session = None  # 当前验收会话
        
    def run_all_checks(self):
        """执行前创建会话"""
        self.session = AcceptanceSession(...)
        
    def export_results(self, fmt: str = "json"):
        """导出结果"""
        if fmt == "feishu":
            return self.session.export_to_feishu_format()
        elif fmt == "csv":
            return self.session.export_to_csv()
```

**测试验证**：
- [ ] 检查后能否看到会话 ID
- [ ] 结果文件是否正确保存
- [ ] 导出格式是否符合飞书表结构

---

## 第二阶段：可靠性测试增强（2-3 周）

### 2.1 动作重复测试（Action Reliability Test）

**当前状态**：
- 断电恢复测试已支持循环
- 缺少单动作、多动作序列重复执行框架

**改进需求**：
```
界面布局：
┌─────────────────────────────────────────────┐
│  可靠性测试                                   │
├─────────────────────────────────────────────┤
│  [选择动作序列 ▼] [新建] [保存] [删除]       │
│                                              │
│  ┌────────────────────────────────────────┐ │
│  │ 序列编辑                                │ │
│  │ ① move_forward (前进 30cm)              │ │
│  │ ② turn_left   (左转 90°)                │ │
│  │ ③ move_forward (前进 30cm)              │ │
│  │                     ↑ ↓ ✕                │ │
│  └────────────────────────────────────────┘ │
│                                              │
│  重复设置：                                   │
│  ├─ 重复次数: [    10  ]                    │
│  ├─ 动作间隔: [    3   ] 秒                 │
│  ├─ □ 自动对齐起点   □ 过程中暂停检查     │
│  │                                          │
│  ├─ [▶ 开始运行]  [⏸ 暂停]  [⏹ 停止]       │
│  │                                          │
│  └─ 进度: 3/10, 已耗时 45s, 预计还需 1m 45s │
│     [◀ 上一步] [下一步 ▶]                   │
│                                              │
│  执行记录：                                   │
│  ┌────────────────────────────────────────┐ │
│  │ 13:45:00 - 周期 1: move_forward ✓       │ │
│  │ 13:45:03 - 周期 1: turn_left ✓         │ │
│  │ 13:45:06 - 周期 1: move_forward ✓      │ │
│  │ 13:45:10 - 周期 2: move_forward ✓      │ │
│  │ ...                                     │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

**关键类设计**：
```python
# ui/widgets/sequencer_editor.py 增强
class SequencerEditor(QWidget):
    """编辑可重复动作序列"""
    def add_action(self, action_name: str):
        """在序列中添加动作"""
    def remove_action(self, index: int):
        """删除指定位置的动作"""
    def move_action(self, from_idx: int, to_idx: int):
        """拖拽调整顺序"""
    def save_sequence(self, name: str):
        """序列保存到数据库"""

# models/action_sequence.py 新增
@dataclass
class ActionSequence:
    name: str  # "验收序列 1"
    actions: list[str]  # ["move_forward", "turn_left", ...]
    created_at: str
    used_count: int = 0

# workers/sequence_executor.py 新增
class SequenceExecutor(QThread):
    step_completed = pyqtSignal(int, int, str, bool)  # 当前周期, 当前步, 动作名, 成功否
    paused = pyqtSignal()
    finished = pyqtSignal(int, dict)  # 总周期, 统计结果
    
    def __init__(self, sequence: ActionSequence, repeat_count: int, interval: int):
        ...
    
    def run(self):
        """顺序执行动作序列"""
        for cycle in range(self.repeat_count):
            if self.should_stop:
                break
            if self.is_paused:
                self.paused.emit()
                self.wait_resume()
            for step, action in enumerate(self.sequence.actions):
                result = self.execute_action(action)
                self.step_completed.emit(cycle+1, step+1, action, result.success)
                if not result.success and self.stop_on_error:
                    break
                time.sleep(self.interval)
```

**测试验证**：
- [ ] 能否创建、保存、加载动作序列
- [ ] 循环执行中是否可暂停、恢复
- [ ] 动作失败时是否能捕获并记录
- [ ] 执行报告是否正确统计

---

### 2.2 可靠性测试报告与分析

**改进需求**：
```python
# 测试完毕后显示报告
@dataclass
class ReliabilityTestReport:
    sequence_name: str
    repeat_count: int
    success_count: int
    failure_count: int
    success_rate: float  # e.g., 95.5%
    
    # 按动作统计
    per_action_stats: dict[str, {
        "success": int,
        "failure": int,
        "avg_duration_ms": float,
        "first_failure_at_cycle": int | None,
    }]
    
    # 故障模式分析
    failure_patterns: list[{
        "action": str,
        "error_type": str,
        "occurred_at_cycles": list[int],
        "last_error_detail": str,
    }]
    
    def export_markdown(self) -> str:
        """生成可读的 Markdown 报告"""
```

---

## 第三阶段：用户体验优化（1-2 周）

### 3.1 验收预设工作区

**改进需求**：
- 预设"地面验收"、"吊装验收"、"应力测试"等工作区
- 快速切换预设时自动配置检查项、安全参数、预期结果

### 3.2 实时日志流与诊断

**改进需求**：
- 验收运行中同步显示机器人实时日志
- 动作失败时自动截取前后 30s 日志进行初步分析

### 3.3 检查项自定义与导入

**改进需求**：
- 支持从飞书表导入验收检查清单
- 允许用户自定义检查项（脚本、SSH 命令）

---

## 实现优先级与工作量评估

| 阶段 | 功能 | 优先级 | 工作量 | 预计周期 |
|------|------|--------|--------|---------|
| 1.1 | 断电循环控制增强 | P0 | M | 3-4 天 |
| 1.2 | 吊装工况保护拦截 | P0 | S | 2 天 |
| 1.3 | 结果记录与导出 | P0 | M | 3 天 |
| 2.1 | 动作序列重复测试 | P1 | L | 5-7 天 |
| 2.2 | 可靠性报告分析 | P1 | M | 4 天 |
| 3.1 | 预设工作区 | P2 | M | 3 天 |
| 3.2 | 实时日志流 | P2 | M | 3-4 天 |
| 3.3 | 自定义检查导入 | P2 | L | 5 天 |

**推荐路线**：
- **立即开始**：1.1 + 1.2 + 1.3（预计 1 周内交付可用版本）
- **后续迭代**：2.1 + 2.2（可靠性测试完整支持）
- **长期规划**：3.x（高度可定制化）

---

## 附录：代码文件清单（待创建）

新增文件：
```
models/
  ├── acceptance_result.py       # 验收结果数据模型
  ├── action_sequence.py         # 动作序列定义
  └── reliability_report.py      # 可靠性测试报告

workers/
  └── sequence_executor.py       # 动作序列执行线程

ui/panels/
  ├── reliability_test_panel.py  # 可靠性测试面板
  └── reliability_report_panel.py # 结果报告展示面板

ui/widgets/
  └── progress_tracker.py        # 进度追踪控件

utils/
  └── result_exporter.py         # 结果导出工具
```

修改文件：
```
ui/panels/power_cycle_panel.py      # 增强循环控制
ui/panels/control_panel.py          # 增加动作拦截
ui/panels/dance_library_panel.py    # 增加动作拦截
ui/panels/acceptance_test_panel.py  # 结果记录与会话管理
ui/widgets/sequencer_editor.py      # 增强序列编辑
config.py                           # 增加风险配置与预设
```

---

## 补充：与飞书表格式的对接建议

当前验收表包含页签：
- 报告首页
- 入库装箱清单
- 接口测试
- 功能检测
- 可靠性测试
- 可靠性后回归验收
- 外观及包装
- 入库形态与要求

**对应关系**：
```
Oli Manager 验收功能        → 飞书页签
─────────────────────────────────────
自动验收（WiFi/HTTP/SSH）  → 接口测试
断电恢复循环              → 功能检测（电源模块）
动作序列重复测试          → 可靠性测试
可靠性报告导出            → 可靠性测试 / 报告首页
```

建议后续补充 API，支持：
- 读取飞书表的检查清单
- 回写验收结果到飞书表的对应单元格

---

**文档版本**：v1.0  
**最后更新**：2026-06-04  
**下一次评审**：完成第一阶段后
