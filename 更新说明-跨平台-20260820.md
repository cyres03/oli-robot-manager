# Oli Robot Manager 跨平台迭代说明 2026-08-20

## 1. Sprint 信息

| 项目 | 内容 |
|------|------|
| 版本 | `1.0.1` |
| Sprint Goal | 让新开发者在不共享明文凭据的前提下运行自动验收，并统一 Windows/Linux 开发与发布流程 |
| 目标平台 | Windows 10/11、Ubuntu 22.04 / Linux x86_64 |
| 主要工作项 | [Issue #4](https://github.com/cyres03/oli-robot-manager/issues/4) |
| 已合并迭代 | [PR #1](https://github.com/cyres03/oli-robot-manager/pull/1)、[PR #3](https://github.com/cyres03/oli-robot-manager/pull/3) |
| 当前状态 | 源码与跨平台 CI 已完成；正式 `v1.0.1` 平台安装包尚未发布 |

---

## 2. 用户问题

同事从私有 GitHub 仓库克隆项目后，自动验收中的 HTTP 项可以直接执行，但 SSH 项需要真实机器人认证。凭据已经按安全要求移出 Git，旧说明容易让开发者误以为必须手工修改 `config.local.json`，且缺少对首次授权后如何继续检查的行为验证。

本迭代将流程调整为：

1. 新开发者安装依赖并启动程序
2. 连接目标机器人 WiFi
3. 点击“运行全部自动检查”
4. SSH 尚未授权时，程序暂停当前检查并弹出密码框
5. 密码仅用于为当前机器人安装本机 SSH 公钥
6. 授权成功后自动重试原检查
7. 后续优先使用 SSH 密钥；密码可选保存到系统凭据管理器

真实密码、私钥和 `config.local.json` 都不会进入 GitHub。

---

## 3. 自动检查更新

### 3.1 首次 SSH 授权

自动验收、健康检查、校零和快捷 SSH 统一使用以下流程：

- 机器人按 `ACCID + IP + 用户名` 隔离 SSH 主机指纹和授权状态
- 无可用 SSH 密钥时显示“等待授权”
- 弹窗请求一次当前账号密码
- 自动创建 `~/.ssh/oli_robot_manager_ed25519`
- 将公钥安装到当前机器人账号的 `authorized_keys`
- 立即使用密钥复验并重试原操作

### 3.2 凭据处理

| 平台 | 安全存储后端 | 不可用时 |
|------|--------------|----------|
| Windows | Windows Credential Manager | 密码仅用于本次操作 |
| Linux | Secret Service / 桌面密钥环 | 密码仅用于本次操作 |

密码保存是可选项。SSH 公钥授权成功后，即使不保存密码，大部分只读自动检查也可继续使用密钥完成。

### 3.3 换机保护

多台机器人可能复用 `10.192.1.2` 和 `10.192.1.3`。授权或检查期间如 ACCID 发生变化：

- 取消原机器人待执行检查
- 不向旧目标继续发送命令
- 在结果中明确显示机器人已切换

### 3.4 自动化回归覆盖

新增自动验收行为测试：

- 未授权 SSH 检查进入“等待授权”
- 授权请求包含主机、账号和 ACCID
- 授权成功后自动重试同一检查
- 授权期间换机时拒绝重试旧目标

这些测试不连接真实机器人，不触发运动、断电或校零。

---

## 4. Linux 版本更新

Linux 详细说明见 [更新说明-Linux-20260819.md](更新说明-Linux-20260819.md)。本次已合并能力包括：

- Ubuntu 22.04 / Python 3.10 源码运行
- PyQt6 Wayland / X11 窗口支持
- Linux 双 WiFi 网卡与 NetworkManager / `nmcli` 识别
- 多机器人 SSH 公钥认证和主机指纹隔离
- Secret Service 安全凭据存储与安全退化
- 感知机时间检查、sudo 密码提示和校时复验
- Linux x86_64 PyInstaller 构建脚本
- Windows/Linux 分平台 GitHub Actions CI 与 Release 工作流

PR #3 额外修复了 Wayland 下首次连接密码弹窗出现黑底或重绘闪烁的问题，并增加透明像素回归测试。

### Linux 发布状态

当前 GitHub `main` 已包含 Linux 源码、构建脚本和更新说明，Windows/Linux CI 均通过。

正式 Linux 便携包尚未出现在 GitHub Release 中，原因是尚未创建与 `VERSION=1.0.1` 对应的 `v1.0.1` Tag。推送该 Tag 后，发布工作流才会构建并上传：

```text
OliRobotManager-Linux-x86_64-v1.0.1.tar.gz
```

因此当前应描述为“Linux 源码与发布链已完成”，不能描述为“Linux 安装包已发布”。

---

## 5. 开发者升级步骤

已有仓库：

```powershell
git switch main
git pull
python -m pip install -r requirements.txt
python main.py
```

Windows 全新克隆：

```powershell
git clone https://github.com/cyres03/oli-robot-manager.git
cd oli-robot-manager
python -m venv .venv
.venv\Scripts\activate.bat
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python main.py
```

Linux 全新克隆：

```bash
git clone https://github.com/cyres03/oli-robot-manager.git
cd oli-robot-manager
chmod +x scripts/linux_setup.sh scripts/linux_run.sh
./scripts/linux_setup.sh
./scripts/linux_run.sh
```

不再要求开发者把真实 SSH 密码写入 `config.local.json`。如需要覆盖默认地址或兼容旧部署，仍可使用本地配置，但文件不得提交。

---

## 6. 验收结果

### GitHub CI

PR #1：

- Windows / Python 3.10：通过
- Linux / Python 3.10：通过

PR #3：

- Windows / Python 3.10：通过
- Linux / Python 3.10：通过

### 本地验证

2026-08-20 在 Windows 开发环境执行：

```text
python -m pip check                                  PASS
python -m compileall -q .                            PASS
QT_QPA_PLATFORM=offscreen python -m pytest -q        PASS
应用模块与凭据后端导入                              PASS
自动验收 SSH 授权行为测试                           2 passed
凭据后端                                             Windows Credential Manager
```

### PR #1 已记录的 Linux 实机验证

- Ubuntu 22.04 / Python 3.10 源码启动
- Wayland 窗口创建
- 双 WiFi 网卡真实热点识别
- 主控和感知机 SSH 公钥登录
- MissionEngine 服务只读检查
- 感知机时间检查返回 PASS
- Secret Service 随机凭据写入、读取、删除和机器人隔离

---

## 7. 未自动触发的高风险操作

本迭代验证没有自动触发：

- 机器人运动命令
- MissionEngine 校零
- Backlash 检测
- 断电恢复循环

这些功能仍需测试人员在明确机器人型号、ACCID、固件和安全工况后手工验收。

---

## 8. 2026-08-26 工作区防抖热修

### 现象

Oli 舞蹈结束前后，软件偶尔从当前工作区跳到“连接设置”，约 5 秒后又自动返回。SDK 审计显示机器人 ACCID 始终未变化。

### 根因

Windows WiFi 身份轮询在舞蹈期间可能偶发一次 `NO_TARGET`。旧逻辑把单次探测丢失当成真实断连，立即清空 Profile 并切换到连接工作区。

### 修复

- 首次 `NO_TARGET` 立即清空 MCP/SSH 控制目标并禁用动作能力，但保留当前工作区和页面
- 连续第二次 `NO_TARGET` 才确认断连并进入连接设置
- 身份在第二次探测前恢复时，重新加载当前机器人资源且不重置页面
- 多机器人、SSID/SN 不一致、未知型号或实际切换机器人仍立即锁定，不参与防抖

自动化测试覆盖瞬时丢失、持续断连、恢复和同工作区导航保持。真机需复测舞蹈结束后不再跳转工作区。

---

## 9. 已知限制与待办

- 正式 `v1.0.1` Windows/Linux Release 尚未发布
- Linux 需要图形桌面、NetworkManager 和可用的 D-Bus 会话
- Secret Service 不可用或密钥环未解锁时，密码不会持久化
- 首次 SSH 公钥安装仍需要对应机器人账号的正确密码
- 当前行为测试覆盖授权编排，不替代真实机器人网络与 SSH 联调

---

## 10. 回滚

如果本次更新阻断现场验收：

1. 退出应用
2. 记录当前提交、平台、机器人 ACCID 和脱敏错误日志
3. 切回上一个已知可用提交或安装包
4. 不删除 `~/.oli-robot-manager`，先备份本地数据
5. 在 GitHub Issue 中附复现步骤并标记目标平台

源码回滚示例：

```powershell
git switch main
git log --oneline
git switch --detach <已知可用提交>
```

问题修复后重新回到：

```powershell
git switch main
git pull
```

---

## 11. Sprint Review 结论

- **完成**：Linux 源码支持、安全 SSH 授权、系统凭据管理、跨平台 CI、Wayland 密码弹窗修复
- **完成**：自动验收首次授权与换机保护行为测试
- **待发布**：创建 `v1.0.1` Tag，生成 Windows/Linux 正式平台资产
- **待真机回归**：由机器人测试人员验证自动验收全部 SSH 项、校时、校零和 Backlash