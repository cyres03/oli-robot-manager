# Oli Robot Manager Linux 更新说明 2026-08-19

## 1. 发布信息

| 项目 | 内容 |
|------|------|
| 版本 | `1.0.1` |
| 平台 | Ubuntu 22.04 / Linux x86_64 |
| 源码分支 | `feat/secure-multi-robot-access` |
| 发布形式 | Linux 便携包 `tar.gz` |

Linux 发布包命名：

```text
OliRobotManager-Linux-<arch>-v<version>.tar.gz
```

本次 x86_64 示例：

```text
release/linux/OliRobotManager-Linux-x86_64-v1.0.1.tar.gz
```

Linux 包与 Windows 的 ZIP / EXE 安装包相互独立，不能跨平台使用。

## 2. Linux 新增能力

### 2.1 Ubuntu 22.04 源码运行

- 支持 Python 3.10 虚拟环境运行。
- 提供 `scripts/linux_setup.sh` 和 `scripts/linux_run.sh`。
- 支持 Wayland / X11 的 PyQt6 桌面窗口。

### 2.2 双 WiFi 网卡机器人识别

- 使用 NetworkManager / `nmcli` 读取 Linux 活动 WiFi。
- 支持内置网卡连接办公网络、外置网卡连接机器人热点。
- 使用当前接入点真实 SSID，不把 NetworkManager 配置名称误认为机器人 SSID。
- 支持 `HU_*`、`WF_TRON2*` 等机器人热点识别。

### 2.3 多机器人 SSH 安全访问

多台机器人复用 `10.192.1.2` 和 `10.192.1.3` 时：

- 按机器人 ACCID、IP 和账号隔离 SSH 主机指纹。
- 新机器人首次使用时输入一次密码并安装操作员公钥。
- 后续 SSH 操作优先使用 `~/.ssh/oli_robot_manager_ed25519`。
- 操作期间检测到机器人切换时，拒绝向旧目标继续发送命令。
- 快捷 SSH 使用当前 ACCID 的严格主机指纹。

### 2.4 系统凭据管理器

- Linux 使用桌面 Secret Service 保存可选密码。
- 密码按 ACCID、IP、用户名隔离。
- 密码不会写入仓库、日志或 `config.local.json`。
- 设置页可查看当前机器人凭据状态并清除密码。
- Secret Service 不可用时，自动退化为仅本次使用。

### 2.5 感知机时间校准

- 自动比较 `10.192.1.3` 与可信北京时间。
- 时间错误时请求一次 sudo 密码。
- 可将 sudo 密码安全保存到 Secret Service。
- 保存密码失效时自动删除并重新提示。
- 校时完成后自动复验时区和时间偏差。

### 2.6 连接状态修复

底部状态栏现在使用实际连接结果更新：

- WiFi
- MCP
- WebSocket
- SSH

## 3. Linux 构建方式

安装前置系统包：

```bash
sudo apt update
sudo apt install -y python3 python3-venv python3-pip network-manager openssh-client \
  libxcb-cursor0 libxcb-xinerama0 libxkbcommon-x11-0
```

构建：

```bash
chmod +x scripts/linux_setup.sh scripts/linux_build_portable.sh \
  scripts/linux_setup_backlash_resource.sh
./scripts/linux_setup_backlash_resource.sh
./scripts/linux_setup.sh
./scripts/linux_build_portable.sh
```

输出：

```text
dist/linux/OliRobotManager/OliRobotManager
release/linux/OliRobotManager-Linux-x86_64-v1.0.1.tar.gz
```

## 4. 安装与运行

```bash
tar -xzf OliRobotManager-Linux-x86_64-v1.0.1.tar.gz
cd OliRobotManager
./OliRobotManager
```

源码运行：

```bash
./scripts/linux_run.sh
```

## 5. 验证结果

已完成：

- Ubuntu 22.04 / Python 3.10 源码启动。
- PyQt6 Wayland 窗口创建。
- 全量 Python 编译。
- `pip check` 依赖一致性检查。
- 双 WiFi 网卡真实热点识别。
- 多机器人 ACCID 识别和换机保护。
- 主控、感知机 SSH 公钥认证。
- MissionEngine 服务只读检查。
- 感知机时间检查真实返回 PASS。
- Secret Service 随机凭据写入、读取、删除和机器人隔离。

未在本次验证中自动触发：

- MissionEngine 校零动作。
- 机器人运动命令。
- Linux PyInstaller 最终便携包构建（发布前由 Linux 构建任务执行）。

## 6. 已知限制

- Linux 需要可用的图形桌面；纯 SSH 无显示环境不能直接打开 PyQt6 窗口。
- WiFi 扫描和连接依赖 NetworkManager / `nmcli`。
- Secret Service 需要桌面 D-Bus 会话和已解锁的密钥环。
- Backlash 运行资源不在 Git 中，需要按项目 Release 说明单独准备。
- Linux 便携包应在与目标机器兼容的 Linux 环境中构建。

## 7. 回滚

如果 Linux 新版出现阻断问题：

1. 退出当前程序。
2. 保留 `~/.oli-robot-manager` 数据目录作为备份。
3. 解压上一版本 Linux 包到新目录。
4. 使用上一版本可执行文件启动。
5. 在 GitHub Issue 中记录版本、平台、复现步骤和脱敏日志。
